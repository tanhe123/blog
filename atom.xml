<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>tanhe123&#39;s blog</title>
  
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://yoursite.com/"/>
  <updated>2017-11-12T10:56:11.246Z</updated>
  <id>http://yoursite.com/</id>
  
  <author>
    <name>tanhe123</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Spring MVC 与 CORS</title>
    <link href="http://yoursite.com/2016/12/20/spring-mvc-cors/"/>
    <id>http://yoursite.com/2016/12/20/spring-mvc-cors/</id>
    <published>2016-12-20T01:12:00.000Z</published>
    <updated>2017-11-12T10:56:11.246Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-CORS-简介"><a href="#1-CORS-简介" class="headerlink" title="1. CORS 简介"></a>1. CORS 简介</h2><p>同源策略（same origin policy）是浏览器安全的基石。在同源策略的限制下，<strong>非同源的网站之间不能发送 ajax 请求的</strong>。</p><p>为了解决这个问题，w3c 提出了跨源资源共享，即 CORS(<a href="https://www.w3.org/TR/cors/" target="_blank" rel="external">Cross-Origin Resource Sharing</a>)。</p><p>CORS 做到了两点：</p><ol><li>不破坏即有规则</li><li>服务器实现了 CORS 接口，就可以跨源通信</li></ol><p>基于这两点，CORS 将请求分为两类：简单请求和非简单请求。</p><a id="more"></a><h3 id="1-1-简单请求"><a href="#1-1-简单请求" class="headerlink" title="1.1 简单请求"></a>1.1 简单请求</h3><p>可以先看下 CORS 出现前的情况：跨源时能够通过 script 或者 image 标签触发 GET 请求或通过表单发送一条 POST 请求，但这两种请求 HTTP 头信息中都不能包含任何自定义字段。</p><p>简单请求对应该规则，因此对简单请求的定义为：</p><p>请求方法是 <code>HEAD</code>、<code>GET</code> 或 <code>POST</code> 且 HTTP 头信息不超过以下几个字段：<code>Accept</code>、<code>Accept-Language</code>、<code>Content-Language</code>、<code>Last-Event-ID</code>、<code>Content-Type</code>（只限于 <code>application/x-www-form-urlencoded</code>、<code>multipart/form-data</code>、<code>text/plain</code>）。</p><p>比如有一个简单请求：</p><figure class="highlight http"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">GET</span> <span class="string">/test</span> HTTP/1.1</span><br><span class="line"><span class="attribute">Accept</span>: */*</span><br><span class="line"><span class="attribute">Accept-Encoding</span>: gzip, deflate, sdch, br</span><br><span class="line"><span class="attribute">Origin</span>: http://www.examples.com</span><br><span class="line"><span class="attribute">Host</span>: www.examples.com</span><br></pre></td></tr></table></figure><p>对于这样的简单请求，CORS 的策略是请求时，<strong>在头信息中添加一个 Origin 字段</strong>，服务器收到请求后，根据该字段判断是否允许该请求。</p><ol><li>如果允许，则在 HTTP 头信息中添加 <code>Access-Control-Allow-Origin</code> 字段，并返回正确的结果</li><li>如果不允许，则不在头信息中添加 <code>Access-Control-Allow-Origin</code> 字段。</li></ol><p>浏览器先于用户得到返回结果，根据有无 <code>Access-Control-Allow-Origin</code> 字段来决定是否拦截该返回结果。</p><p>对于 CORS 出现前的一些服务，CORS 对他们的影响分两种情况：</p><ol><li>script 或者 image 触发的 GET 请求不包含 Origin 头，所以不受到 CORS 的限制，依旧可用。</li><li>如果是 ajax 请求，HTTP 头信息中会包含 Origin 字段，由于服务器没有做任何配置，所以返回结果不会包含 <code>Access-Control-Allow-Origin</code>，因此返回结果会被浏览器拦截，接口依旧不可以被 ajax 跨源访问。</li></ol><p>可以看出，CORS 的出现，没有对”旧的“服务造成任何影响。</p><p>另外，除了提到的 <code>Access-Control-Allow-Origin</code> 还有几个字段用于描述 CORS 返回结果：</p><ol><li>Access-Control-Allow-Credentials: 可选，用户是否可以发送、处理 cookie。</li><li>Access-Control-Expose-Headers：可选，可以让用户拿到的字段。有几个字段无论设置与否都可以拿到的，包括：Cache-Control、Content-Language、Content-Type、Expires、Last-Modified、Pragma。</li></ol><h3 id="1-2-非简单请求"><a href="#1-2-非简单请求" class="headerlink" title="1.2 非简单请求"></a>1.2 非简单请求</h3><p>除了简单请求之外的请求，就是非简单请求。</p><p>对于非简单请求的跨源请求，<strong>浏览器会在真实请求发出前</strong>，增加一次 OPTION 请求，称为预检请求（preflight request）。预检请求将真实请求的信息，包括请求方法、自定义头字段、源信息添加到 HTTP 头信息字段中，询问服务器是否允许这样的操作。</p><p>比如对于 <code>DELETE</code> 请求：</p><figure class="highlight oxygene"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">OPTIONS /test HTTP/<span class="number">1.1</span></span><br><span class="line">Origin: http:<span class="comment">//www.examples.com</span></span><br><span class="line">Access-Control-Request-<span class="function"><span class="keyword">Method</span>:</span> DELETE</span><br><span class="line">Access-Control-Request-Headers: X-Custom-Header</span><br><span class="line">Host: www.examples.com</span><br></pre></td></tr></table></figure><p>与 CORS 相关的字段有：</p><ol><li><code>Access-Control-Request-Method</code>: 真实请求使用的 HTTP 方法。</li><li><code>Access-Control-Request-Headers</code>: 真实请求中包含的自定义头字段。</li></ol><p>服务器收到请求时，需要分别对 Origin、Access-Control-Request-Method、Access-Control-Request-Headers 进行验证，验证通过后，会在返回 Http 头信息中添加</p><figure class="highlight groovy"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Access-Control-Allow-<span class="string">Origin:</span> <span class="string">http:</span><span class="comment">//www.examples.com</span></span><br><span class="line">Access-Control-Allow-<span class="string">Methods:</span> GET, POST, PUT, DELETE</span><br><span class="line">Access-Control-Allow-<span class="string">Headers:</span> X-Custom-Header</span><br><span class="line">Access-Control-Allow-<span class="string">Credentials:</span> <span class="literal">true</span></span><br><span class="line">Access-Control-Max-<span class="string">Age:</span> <span class="number">1728000</span></span><br></pre></td></tr></table></figure><p>他们的含义分别是：</p><ol><li>Access-Control-Allow-Methods: 真实请求允许的方法</li><li>Access-Control-Allow-Headers: 服务器允许使用的字段</li><li>Access-Control-Allow-Credentials: 是否允许用户发送、处理 cookie</li><li>Access-Control-Max-Age: 预检请求的有效期，单位为秒。有效期内，不会重复发送预检请求</li></ol><p>当预检请求通过后，浏览器会发送真实请求到服务器。这就实现了跨源请求。</p><p>了解完 CORS，接下来我们来搭建简单的 Spring MVC 服务，并进一步了解 Spring MVC 如何配置 CORS。</p><h2 id="2-Spring-MVC-环境搭建"><a href="#2-Spring-MVC-环境搭建" class="headerlink" title="2. Spring MVC 环境搭建"></a>2. Spring MVC 环境搭建</h2><p>打开 <a href="http://start.spring.io/" target="_blank" rel="external">http://start.spring.io/</a>，添加 Web Dependency，然后选择 Generate Project，下载 zip 文件，就得到了一个 spring boot demo。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/30106795-badd-48f2-93b1-cd834b94fc18.png" alt="图片"> </p><p>解压 zip 文件，双击 pom.xml 打开或用 IDEA、Eclipse 将项目按照 maven 导入。</p><p>根据 Group、Artifact、Dependencies 填写的不同，项目目录结构可能有些许差别，我的项目结构如下：</p><figure class="highlight 1c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">├── src</span><br><span class="line">│   ├── main/java</span><br><span class="line">│   <span class="string">|        └── net/xiayule/spring/cors</span></span><br><span class="line">│   <span class="string">|            └── SpringBootCorsTestApplication.java</span></span><br><span class="line"><span class="string">|   └── resources</span></span><br><span class="line"><span class="string">|       ├── static</span></span><br><span class="line"><span class="string">|       ├── templates</span></span><br><span class="line"><span class="string">|       └── application.properties</span></span><br><span class="line"><span class="string">|    </span></span><br><span class="line">└── pom.xml</span><br></pre></td></tr></table></figure><p>我们需要关心的只有 SpringBootCorsTestApplication.java。在 SpringBootCorsTestApplication.java 添加以下代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SpringBootCorsTestApplication</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@RequestMapping</span>(value = <span class="string">"/test"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">greetings</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"&#123;\"project\":\"just a test\"&#125;"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">SpringApplication.run(SpringBootCorsTestApplication.class, args);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>@RequestMapping(value = &quot;/test&quot;)</code> 的含义是该方法接受来自 <code>/test</code> 的请求，并且提供了对 HTTP 的 GET、POST、DELETE 等方法的支持。</p><p>运行项目的方法为，在项目根目录执行 <code>mvn spring-boot:run</code> 启动应用，打开浏览器访问 <code>http://localhost:8080</code> 即可看到效果：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/797a0630-a8f9-416f-9fd5-b3b88fd34b36.png" alt="图片"> </p><p>后端服务搭建好了，接着实现前端。为了简单，直接创建一个 test.html 文件，添加以下内容：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Hello CORS<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"https://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="undefined"></span></span><br><span class="line"><span class="javascript">        $(<span class="built_in">document</span>).ready(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span></span><br><span class="line"><span class="javascript">            $.ajax(&#123;</span></span><br><span class="line"><span class="actionscript">                url: <span class="string">"http://localhost:8080/test"</span>,</span></span><br><span class="line"><span class="actionscript">                method: <span class="string">"POST"</span>,</span></span><br><span class="line"><span class="actionscript">                contentType: <span class="string">"application/json; charset=utf-8"</span></span></span><br><span class="line"><span class="actionscript">            &#125;).then(<span class="function"><span class="keyword">function</span><span class="params">(data, status, jqxhr)</span> </span>&#123;</span></span><br><span class="line"><span class="undefined">                alert(data)</span></span><br><span class="line"><span class="undefined">            &#125;);</span></span><br><span class="line"><span class="undefined">        &#125;);</span></span><br><span class="line"><span class="undefined">    </span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>使用浏览器打开该文件，就会触发一条向 <code>http://localhost:8080</code> 的请求。可以通过修改上面代码的 method，来触发不同类型的请求。</p><p>由于是直接使用浏览器打开，<strong>网页的源为 null</strong>, 当向源 <code>http://localhost:8080</code> 请求时，就变成了跨源请求，因此如果后端不加 CORS 的配置，返回的 HTTP 头信息中不会包含 <code>Access-Control-Allow-Origin</code>，因此浏览器会报出如下错误：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/6c385f02-3c25-4734-9613-792ccef80b15.png" alt="图片"> </p><h2 id="3-配置-CORS"><a href="#3-配置-CORS" class="headerlink" title="3. 配置 CORS"></a>3. 配置 CORS</h2><p>一个应用可能会有多个 CORS 配置，并且可以设置每个 CORS 配置针对一个接口或一系列接口或者对所有接口生效。</p><p>举例来说，我们需要：</p><ol><li>让 /test 接口支持跨源访问，而 /test/1 或 /api 等其它接口不支持跨源访问</li><li>让 /test/* 这一类接口支持跨源访问，而 /api 等其它接口不支持跨源访问</li><li>站点所有的接口都支持跨源访问</li></ol><p>对第一种情况，如果想要对某一接口配置 CORS，可以在方法上添加 CrossOrigin 注解：</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@CrossOrigin(origins = &#123;<span class="meta-string">"http://localhost:9000"</span>, <span class="meta-string">"null"</span>&#125;)</span></span><br><span class="line"><span class="meta">@RequestMapping(value = <span class="meta-string">"/test"</span>, method = RequestMethod.GET)</span></span><br><span class="line"><span class="keyword">public</span> String greetings() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"&#123;\"project\":\"just a test\"&#125;"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>第二种情况，如果想对一系列接口添加 CORS 配置，可以在类上添加注解，对该类声明所有接口都有效：</p><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">CrossOrigin</span>(origins = &#123;"<span class="attribute">http</span>:<span class="comment">//localhost:9000", "null"&#125;)</span></span><br><span class="line"><span class="variable">@RestController</span></span><br><span class="line"><span class="variable">@SpringBootApplication</span></span><br><span class="line">public class SpringBootCorsTestApplication &#123;</span><br><span class="line">    <span class="comment">// xxx</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>第三种情况，添加全局配置，则需要添加一个配置类：</p><figure class="highlight accesslog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">@Configuration</span><br><span class="line">public class WebConfig extends WebMvcConfigurerAdapter &#123;</span><br><span class="line"></span><br><span class="line">@Override</span><br><span class="line">public void addCorsMappings(CorsRegistry registry) &#123;</span><br><span class="line">registry.addMapping(<span class="string">"/**"</span>)</span><br><span class="line">.allowedOrigins(<span class="string">"http://localhost:9000"</span>, <span class="string">"null"</span>)</span><br><span class="line">.allowedMethods(<span class="string">"<span class="keyword">POST</span>"</span>, <span class="string">"<span class="keyword">GET</span>"</span>, <span class="string">"<span class="keyword">PUT</span>"</span>, <span class="string">"<span class="keyword">OPTIONS</span>"</span>, <span class="string">"<span class="keyword">DELETE</span>"</span>)</span><br><span class="line">.maxAge(<span class="number">3600</span>)</span><br><span class="line">.allowCredentials(true);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>另外，还可以通过添加 Filter 的方式，配置 CORS 规则，并手动指定对哪些接口有效。</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">@<span class="keyword">Bean</span></span><br><span class="line"><span class="keyword">public </span>FilterRegistrationBean corsFilter() &#123;</span><br><span class="line">UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource()<span class="comment">;</span></span><br><span class="line">CorsConfiguration <span class="built_in">config</span> = new CorsConfiguration()<span class="comment">;</span></span><br><span class="line"><span class="built_in">config</span>.setAllowCredentials(true)<span class="comment">;config.addAllowedOrigin("http://localhost:9000");</span></span><br><span class="line"><span class="built_in">config</span>.<span class="keyword">addAllowedOrigin("null");</span></span><br><span class="line"><span class="keyword"></span><span class="built_in">config</span>.<span class="keyword">addAllowedHeader("*");</span></span><br><span class="line"><span class="keyword"></span><span class="built_in">config</span>.<span class="keyword">addAllowedMethod("*");</span></span><br><span class="line"><span class="keyword"></span>source.registerCorsConfiguration(<span class="string">"/**"</span>, <span class="built_in">config</span>)<span class="comment">; // CORS 配置对所有接口都有效</span></span><br><span class="line">FilterRegistrationBean <span class="keyword">bean </span>= newFilterRegistrationBean(new CorsFilter(source))<span class="comment">;</span></span><br><span class="line"><span class="keyword">bean.setOrder(0);</span></span><br><span class="line"><span class="keyword"></span>return <span class="keyword">bean;</span></span><br><span class="line"><span class="keyword">&#125;</span></span><br></pre></td></tr></table></figure><h2 id="4-实现剖析"><a href="#4-实现剖析" class="headerlink" title="4. 实现剖析"></a>4. 实现剖析</h2><p>无论是通过哪种方式配置 CORS，其实都是在构造 CorsConfiguration。<br>一个 CORS 配置用一个 <code>CorsConfiguration</code> 类来表示，它的定义如下：</p><figure class="highlight lasso"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> class CorsConfiguration &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="built_in">List</span>&lt;<span class="built_in">String</span>&gt; allowedOrigins;</span><br><span class="line">    <span class="keyword">private</span> <span class="built_in">List</span>&lt;<span class="built_in">String</span>&gt; allowedMethods;</span><br><span class="line">    <span class="keyword">private</span> <span class="built_in">List</span>&lt;<span class="built_in">String</span>&gt; allowedHeaders;</span><br><span class="line">    <span class="keyword">private</span> <span class="built_in">List</span>&lt;<span class="built_in">String</span>&gt; exposedHeaders;</span><br><span class="line">    <span class="keyword">private</span> <span class="built_in">Boolean</span> allowCredentials;</span><br><span class="line">    <span class="keyword">private</span> Long maxAge;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Spring MVC 中对 CORS 规则的校验，都是通过委托给 DefaultCorsProcessor 实现的。</p><p>DefaultCorsProcessor 处理过程如下：</p><ol><li>判断依据是 Header 中是否包含 Origin。如果包含则说明为 CORS 请求，转到 2；否则，说明不是 CORS 请求，不作任何处理。</li><li>判断 response 的 Header 是否已经包含 Access-Control-Allow-Origin，如果包含，证明已经被处理过了, 转到 3，否则不再处理。</li><li>判断是否同源，如果是则转交给负责该请求的类处理</li><li>是否配置了 CORS 规则，如果没有配置，且是预检请求，则拒绝该请求，如果没有配置，且不是预检请求，则交给负责该请求的类处理。如果配置了，则对该请求进行校验。</li></ol><p>校验就是根据 CorsConfiguration 这个类的配置进行判断：</p><ol><li>判断 origin 是否合法</li><li>判断 method 是否合法</li><li>判断 header 是否合法</li><li>如果全部合法，则在 response header 中添加响应的字段，并交给负责该请求的类处理，如果不合法，则拒绝该请求。</li></ol><h2 id="5-总结"><a href="#5-总结" class="headerlink" title="5. 总结"></a>5. 总结</h2><p>本文介绍了 CORS 的知识以及如何在 Spring MVC 中配置 CORS。</p><p><a href="https://code.csdn.net/tanheaishui/springbootcorstest/repository/archive?ref=master" target="_blank" rel="external">源码下载</a></p><h2 id="6-参考"><a href="#6-参考" class="headerlink" title="6. 参考"></a>6. 参考</h2><p><a href="https://spring.io/understanding/CORS" target="_blank" rel="external">https://spring.io/understanding/CORS</a><br><a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/Access_control_CORS" target="_blank" rel="external">https://developer.mozilla.org/en-US/docs/Web/HTTP/Access_control_CORS</a><br><a href="http://stackoverflow.com/questions/8685678/cors-how-do-preflight-an-httprequest" target="_blank" rel="external">http://stackoverflow.com/questions/8685678/cors-how-do-preflight-an-httprequest</a><br><a href="http://stackoverflow.com/questions/15381105/cors-what-is-the-motivation-behind-introducing-preflight-requests" target="_blank" rel="external">http://stackoverflow.com/questions/15381105/cors-what-is-the-motivation-behind-introducing-preflight-requests</a><br><a href="http://stackoverflow.com/questions/39725955/why-is-there-no-preflight-in-cors-for-post-requests-with-standard-content-type" target="_blank" rel="external">http://stackoverflow.com/questions/39725955/why-is-there-no-preflight-in-cors-for-post-requests-with-standard-content-type</a><br><a href="https://developer.mozilla.org/zh-CN/docs/Web/Security/Same-origin_policy" target="_blank" rel="external">https://developer.mozilla.org/zh-CN/docs/Web/Security/Same-origin_policy</a><br><a href="http://www.ruanyifeng.com/blog/2016/04/cors.html" target="_blank" rel="external">http://www.ruanyifeng.com/blog/2016/04/cors.html</a><br><a href="https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Access_control_CORS" target="_blank" rel="external">https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Access_control_CORS</a><br><a href="http://www.ruanyifeng.com/blog/2016/04/same-origin-policy.html" target="_blank" rel="external">http://www.ruanyifeng.com/blog/2016/04/same-origin-policy.html</a><br><a href="https://spring.io/blog/2015/06/08/cors-support-in-spring-framework" target="_blank" rel="external">https://spring.io/blog/2015/06/08/cors-support-in-spring-framework</a></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;1-CORS-简介&quot;&gt;&lt;a href=&quot;#1-CORS-简介&quot; class=&quot;headerlink&quot; title=&quot;1. CORS 简介&quot;&gt;&lt;/a&gt;1. CORS 简介&lt;/h2&gt;&lt;p&gt;同源策略（same origin policy）是浏览器安全的基石。在同源策略的限制下，&lt;strong&gt;非同源的网站之间不能发送 ajax 请求的&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;为了解决这个问题，w3c 提出了跨源资源共享，即 CORS(&lt;a href=&quot;https://www.w3.org/TR/cors/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;Cross-Origin Resource Sharing&lt;/a&gt;)。&lt;/p&gt;
&lt;p&gt;CORS 做到了两点：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;不破坏即有规则&lt;/li&gt;
&lt;li&gt;服务器实现了 CORS 接口，就可以跨源通信&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;基于这两点，CORS 将请求分为两类：简单请求和非简单请求。&lt;/p&gt;
    
    </summary>
    
      <category term="spring" scheme="http://yoursite.com/categories/spring/"/>
    
    
      <category term="spring" scheme="http://yoursite.com/tags/spring/"/>
    
      <category term="CORS" scheme="http://yoursite.com/tags/CORS/"/>
    
  </entry>
  
  <entry>
    <title>深入 Spring 系列之静态资源处理</title>
    <link href="http://yoursite.com/2016/10/12/spring-static-resource-process/"/>
    <id>http://yoursite.com/2016/10/12/spring-static-resource-process/</id>
    <published>2016-10-12T01:12:00.000Z</published>
    <updated>2017-11-12T10:55:09.696Z</updated>
    
    <content type="html"><![CDATA[<h1 id="1-背景"><a href="#1-背景" class="headerlink" title="1. 背景"></a>1. 背景</h1><p>前一段时间，WebIDE 开源的过程中，无意间接触到 <a href="http://www.webjars.org/" target="_blank" rel="external">webjars</a>，觉得比较有趣，于是研究并整理了一下。</p><p>webjars 是将前端的库（比如 jQuery）打包成 Jar 文件，然后使用基于 JVM 的包管理器（比如 Maven、Gradle 等）管理前端依赖的方案。</p><p>webjars 的效果非常神奇。对于其用法，我们可以在 maven 项目中添加下面的依赖：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.webjars<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jquery<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.1.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>然后通过请求 <code>http://localhost:8080/webjars/jquery/3.1.0/jquery.js</code> 即可正确访问到 jquery 文件。</p><a id="more"></a><p>可以再举一个应用场景的例子，比如项目要添加 Api 文档，决定使用 Swagger，<a href="http://petstore.swagger.io/" target="_blank" rel="external">demo 参见</a>。效果如图：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/4d1a8ea6-eec1-444f-a99a-a6fd5c324847.png" alt="图片"> </p><p>该框架有两部分，一部分是 springfox-swagger2 提供后端实现，另一部分是 springfox-swagger-ui 提供前端实现。引入后端实现很简单，加入 maven 依赖即可，但是引入 springfox-swagger-ui 麻烦一些。</p><ul><li><p>一种方式是将该项目编译后的 <a href="https://github.com/swagger-api/swagger-ui/tree/master/dist" target="_blank" rel="external">source</a> 加入到项目。这种方式虽然能达到效果，但版本的升级就成了问题，需要手工维护。</p></li><li><p>另一种方式就是我们提到的 webjars 了。去 <a href="http://www.webjars.org/" target="_blank" rel="external">webjars 官网</a>、<a href="http://mvnrepository.com/" target="_blank" rel="external">maven 仓库</a>、<a href="https://github.com/swagger-api" target="_blank" rel="external">官方文档</a> 都可以查到 swagger-ui 依赖。将依赖加入 pom.xml 后，不需要对前端进行任何配置、修改即可引入前端代码。代码的更新也很方便，修改依赖版本号即可。</p></li></ul><p>经过研究才发现，webjars 这并非新的技术，而是利用现有的框架对静态资源的处理方案实现的。接下来我们一起看看 webjars 的实现以及静态资源处理的设计方案。</p><h1 id="2-预备知识"><a href="#2-预备知识" class="headerlink" title="2. 预备知识"></a>2. 预备知识</h1><h2 id="2-1-Servlet-3"><a href="#2-1-Servlet-3" class="headerlink" title="2.1 Servlet 3"></a>2.1 Servlet 3</h2><p>我们可以先来看一下 jquery webjar 的包结构：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">jquery-3</span><span class="selector-class">.1</span><span class="selector-class">.0</span><span class="selector-class">.jar</span></span><br><span class="line">    └─ <span class="selector-tag">META-INF</span></span><br><span class="line">        └─ <span class="selector-tag">resources</span></span><br><span class="line">            └─ <span class="selector-tag">webjars</span></span><br><span class="line">                └─ <span class="selector-tag">jquery</span></span><br><span class="line">                    └─ 3<span class="selector-class">.1</span><span class="selector-class">.0</span></span><br><span class="line">                        └─ <span class="selector-tag">jquery</span><span class="selector-class">.js</span></span><br></pre></td></tr></table></figure><p>拿 Servlet 3 举例，应用打成 war 后，Jar（包括 WebJars）会被放在 <code>WEB-INF/lib</code> 目录下，而 Servlet 3 允许直接访问 <code>WEB-INF/lib</code> 下 jar 中的 <code>/META-INF/resources</code> 目录下的资源。简单来说就是 <code>WEB-INF/lib/{\*.jar}/META-INF/resources</code> 下的资源可以被直接访问。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/00f2a3a4-d342-44ec-ab52-35738faf9e75.png" alt="图片"></p><p>所以对于 Servlet 3，直接使用 <code>http://localhost:8080/webjars/jquery/3.1.0/jquery.js</code> 即可访问到 webjar 中的 jquery.js，而不用做其它的配置。 </p><p>那么如何在 Spring MVC 中访问 webjars 呢？或者说，Spring MVC 如何处理静态资源？</p><h2 id="2-2-Spring-MVC"><a href="#2-2-Spring-MVC" class="headerlink" title="2.2 Spring MVC"></a>2.2 Spring MVC</h2><p>Spring MVC 的入口是 DispatcherServlet，所有的请求都会汇集于该类，而后分发给不同的处理类。如果不做额外的配置，是无法访问静态资源的。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/d5b35f32-595e-45e8-b679-5193ba6d27f0.png" alt="图片"> </p><p>如果想让 Dispatcher Servlet 直接可以访问到静态资源，最简单的方法当然是交给默认的 Servlet。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/0a8ef4d8-4a06-470a-bed6-a449cb36b516.png" alt="图片"> </p><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableWebMvc</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">WebConfig</span> <span class="keyword">extends</span> <span class="title">WebMvcConfigurerAdapter</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    public void configureDefaultServletHandling(<span class="type">DefaultServletHandlerConfigurer</span> configurer) &#123;</span><br><span class="line">        configurer.enable();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这种情况下 Spring MVC 对资源的处理与 Servlet 方式相同。</p><h1 id="3-基础"><a href="#3-基础" class="headerlink" title="3. 基础"></a>3. 基础</h1><p>我们可以通过很简单的配置使得 Spring MVC 有能力处理对静态资源进行处理。</p><p>在 Spring MVC 中，资源的查找、处理使用的是责任链设计模式（Filter Chain）：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/68f17b84-df51-437a-88ed-aa1f4174d5fe.png" alt="图片"> </p><p>其思路为如果当前 resolver 找不到资源，则转交给下一个 resolver 处理。 当前 resolver 找到资源则立即返回给上级 resovler（如果存在），此时上级 resolver 又可以选择对资源进一步处理或再次返回给它的上级（如果存在）。</p><p>配置方法为重写 WebMvcConfigurerAdapter 类的 addResourceHandlers。</p><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableWebMvc</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">WebConfig</span> <span class="keyword">extends</span> <span class="title">WebMvcConfigurerAdapter</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">public void addResourceHandlers(<span class="type">ResourceHandlerRegistry</span> registry) &#123;</span><br><span class="line">    registry.addResourceHandler(<span class="string">"/webjars/**"</span>)</span><br><span class="line">                .addResourceLocations(</span><br><span class="line">                        <span class="string">"classpath:/META-INF/resources/webjars/"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过这样的配置，就成功添加了一个 <code>PathResourceResolver</code>。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/7e202673-8db6-445d-8384-a7d0fa3aeb7d.png" alt="图片"> </p><p>该 resolver 的作用是将 url 为 <code>/webjars/**</code> 的请求映射到 <code>classpath:/META-INF/resources/webjars/</code>。</p><p>比如请求 <code>http://localhost:8080/webjars/jquery/3.1.0/jquery.js</code> 时， Spring MVC 会查找路径为 <code>classpath:/META-INF/resources/webjars/jquery/3.1.0/jquery.js</code> 的资源文件。</p><h1 id="4-进阶"><a href="#4-进阶" class="headerlink" title="4. 进阶"></a>4. 进阶</h1><h2 id="4-1-为静态资源添加版本号"><a href="#4-1-为静态资源添加版本号" class="headerlink" title="4.1 为静态资源添加版本号"></a>4.1 为静态资源添加版本号</h2><p>为了简单起见，我们假设静态资源存放在 <code>classpath:/static</code>，且映射的 url 为 <code>/static</code>。</p><figure class="highlight scala"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableWebMvc</span></span><br><span class="line">public <span class="class"><span class="keyword">class</span> <span class="title">WebConfig</span> <span class="keyword">extends</span> <span class="title">WebMvcConfigurerAdapter</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    public void addResourceHandlers(<span class="type">ResourceHandlerRegistry</span> registry) &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 映射 /static 的请求到 classpath 下的 static 目录</span></span><br><span class="line"></span><br><span class="line">    registry.addResourceHandler(<span class="string">"/static/**"</span>)</span><br><span class="line">                .addResourceLocations(<span class="string">"classpath:/static"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>比如，请求 <code>/static/style.css</code>, 则会直接查找 <code>classpath:/static/style.css</code>。</p><p>我们刚才说到，这段代码实际上是添加了一个 PathResourceResolver，来完成对资源的查找，那么我们是不是可以继续向 Resolver Chain 添加更多的 Resource Resolver，从而实现对静态资源更多样化的处理呢？</p><p>答案是肯定的，接下来，我们添加 VersionResourceResolver。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/37f1d337-d5e4-4453-bc81-736acc346f14.png" alt="图片"> </p><p>VersionResourceResolver 可以为资源添加版本号。其所作的工作如下：首先使用下一个 resolver 获取资源，如果找到资源则返回，不做其它处理；如果 下一个 resolver 找不到资源，则尝试去掉 url 中的 version 信息，重新调用下一个 resolver 处理，然后无论下一个 resolver 能否处理，都返回其结果。</p><p>版本号的策略有两种，下面分别阐述。</p><h2 id="4-1-1-指定版本号"><a href="#4-1-1-指定版本号" class="headerlink" title="4.1.1 指定版本号"></a>4.1.1 指定版本号</h2><p>指定固定值作为版本号，比如：</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">addResourceHandlers</span><span class="params">(ResourceHandlerRegistry registry)</span> </span>&#123;</span><br><span class="line">   registry.addResourceHandler(<span class="string">"/static/**"</span>).addResourceLocations(<span class="string">"classpath:/static"</span>)</span><br><span class="line">           <span class="comment">// resourceChain(false) 的作用后面会讲解</span></span><br><span class="line">           .resourceChain(<span class="keyword">false</span>)</span><br><span class="line">           <span class="comment">// 添加 VersionResourceResolver，且指定版本号</span></span><br><span class="line">           .addResolver(<span class="keyword">new</span> VersionResourceResolver()</span><br><span class="line">               .addFixedVersionStrategy(<span class="string">"1.0.0"</span>, <span class="string">"/**"</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样，在请求资源时，加上 <code>/1.0.0</code> 前缀，即 <code>http://localhost:8080/static/1.0.0/style.css</code> 也可正确访问。</p><p>VersionResourceResolver 在处理该请求时，首先使用 PathResourceResolver 按照配置的映射关系 <code>&quot;/static/**&quot; =&gt; &quot;classpath:/static&quot;</code> 处理，即查找文件 <code>classpath:/static/1.0.0/style.css</code>。由于该文件不存在，VersionResourceResolver 尝试去掉版本号 1.0.0，然后再次查找 <code>classpath:/static/style.css</code>，找到文件，直接返回。</p><h2 id="4-1-2-使用-MD5-作为版本号"><a href="#4-1-2-使用-MD5-作为版本号" class="headerlink" title="4.1.2 使用 MD5 作为版本号"></a>4.1.2 使用 MD5 作为版本号</h2><p>除了指定版本号，也可以使用资源的 MD5 作为其版本号，配置方法为：</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">addResourceHandlers</span><span class="params">(ResourceHandlerRegistry registry)</span> </span>&#123;</span><br><span class="line">        registry.addResourceHandler(<span class="string">"/static/**"</span>)</span><br><span class="line">                .addResourceLocations(<span class="string">"classpath:/static/"</span>)</span><br><span class="line">                .resourceChain(<span class="keyword">false</span>)</span><br><span class="line">                .addResolver(<span class="keyword">new</span> VersionResourceResolver()</span><br><span class="line">                    .addContentVersionStrategy(<span class="string">"/**"</span>));</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p>这样，请求资源时，加上资源的 md5，即 <code>http://localhost:8080/static/style-dfbe630979d120fe54a50593f2621225.css</code> 也可正确访问。</p><p>由于使用资源的 MD5 作为版本号，是 VersionResourceResolver 的其中一种策略，因此与指定版本号的处理方式相同，不再阐述。</p><h2 id="4-2-gzip-压缩"><a href="#4-2-gzip-压缩" class="headerlink" title="4.2 gzip 压缩"></a>4.2 gzip 压缩</h2><p>很多时候，为了降低传输的数据量，可以对资源进行压缩。比如可以将 style.css 压缩成 style.css.gz，但是如何让 Spring MVC 在处理对 style.css 的请求时能正确返回 style.css.gz 呢？</p><p>为了解决这个问题，我们可以继续添加一个 Resource Resolver —— GzipResourceResolver。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/11e15667-ecca-47be-9168-f28a3f5fe170.png" alt="图片"> </p><p>GzipResourceResolver 用来查找资源的压缩版本，它首先使用下一个 Resource Resolver 查找资源，如果可以找到，则再尝试查找该资源的 gzip 版本。如果存在 gzip 版本则返回 gzip 版本的资源，否则返回非 gzip 版本的资源。</p><p>比如对于如下的资源：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">static</span><br><span class="line">    └─ style.css</span><br><span class="line">    └─ style<span class="selector-class">.css</span><span class="selector-class">.gz</span> (使用 gzip 压缩)</span><br></pre></td></tr></table></figure><p>在请求 <code>/static/style.css</code> 时，会先使用 PathResourceResolver 查找 style.css，找到后则再次查找 style.css.gz。这里该文件是存在的，因此会返回 style.css.gz 的内容。</p><p><strong>PS： 请求头中的 Content-Encoding 要包含 gzip</strong></p><p>配置 GzipResourceResolver 很简单：</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">addResourceHandlers</span><span class="params">(ResourceHandlerRegistry registry)</span> </span>&#123;</span><br><span class="line">   registry.addResourceHandler(<span class="string">"/static/**"</span>)</span><br><span class="line">           .addResourceLocations(<span class="string">"classpath:/static/"</span>)</span><br><span class="line">           .resourceChain(<span class="keyword">false</span>)</span><br><span class="line">           .addResolver(<span class="keyword">new</span> GzipResourceResolver())</span><br><span class="line">           .addResolver(<span class="keyword">new</span> VersionResourceResolver().addContentVersionStrategy(<span class="string">"/**"</span>))；</span><br><span class="line">           </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="4-3-chain-cache"><a href="#4-3-chain-cache" class="headerlink" title="4.3 chain cache"></a>4.3 chain cache</h2><p>从上面的情况可以看出，Spring MVC 会对资源进行较多的处理。如果每一次请求都做这些处理，无疑会降低服务器的性能。为了避免这种情况，这时可以添加 CachingResourceResolver 来解决这种问题。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/7524d593-6b09-4ecc-9d4e-873b12c353f1.png" alt="图片"> </p><p>CachingResourceResolver 用于缓存其它 Resource Resolver 查找到的资源。因此 CachingResourceResolver 会被放在最外层。请求先到达 CachingResourceResolver，尝试在缓存中查找，如果找到，则直接返回，如果找不到，则依次调用后面的 resolver，直到有一个 resolver 能够找到资源，CachingResourceResolver 将找到的资源缓存起来，下次请求同样的资源时，就可以从缓存中取了。</p><p>可能有人会担心缓存资源会占用太多的内存。但实际上并没有资源内容，仅仅是对资源的路径（或者说资源的抽象）进行了缓存。</p><p>开启缓存的方法很简单：</p><figure class="highlight actionscript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">.requestChain(<span class="literal">true</span>)</span><br></pre></td></tr></table></figure><p>前面的例子中都选择关闭 chain cache，原因是缓存的存在会增加调试的难度。因此开发时可以考虑关闭该功能。</p><h2 id="4-4-省略-webjar-版本"><a href="#4-4-省略-webjar-版本" class="headerlink" title="4.4 省略 webjar 版本"></a>4.4 省略 webjar 版本</h2><p><code>AbstractResourceResolver</code> 的子类一共有 5 个，我们已经提到了 4 个。最后一个是 WebJarsResourceResolver。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/3e58d9a3-c418-4db5-83e9-5759ba856650.png" alt="图片"> </p><p>WebJarsResourceResolver 并不需要手动添加。WebJarsResourceResolver 依赖了 <code>webjars-locator</code> 包，因此当添加了 <code>webjars-locator</code> 依赖时，Spring MVC 会自动添加 WebJarsResourceResolver。</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.webjars<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>webjars-locator<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.32<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><p>WebJarsResourceResolver 的作用是可以省略 webjar 的版本。比如对于请求 <code>http://localhost:8080/webjars/jquery/3.1.0/jquery.js</code> 省略版本号 3.1.10 直接使用 <code>http://localhost:8080/webjars/jquery/jquery.js</code> 也可访问。</p><p>至此所有 Spring MVC 提供的 ResourceResolver 都讲完了。Spring MVC 提供的这 4 个 ResourceResolver 基本够用，如果不能满足业务需求，也可以自定义 ResourceResolver 来满足需求。</p><h2 id="4-5-Transformer"><a href="#4-5-Transformer" class="headerlink" title="4.5 Transformer"></a>4.5 Transformer</h2><p>实际上，除了 ResourceResolver，Spring MVC 还支持修改资源内容，即 Resource Transformer。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/a33bf0d8-1c6c-4d72-b45f-2318d75e6a1c.png" alt="图片"> </p><p>可用的 Resource Transformer 有以下几个：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/ec68ef1e-e17a-4021-bbd6-3698538b164f.png" alt="图片"> </p><p>他们的功能依次为：</p><ul><li><strong>AppCacheManifestTransformer:</strong> 帮助处理 HTML5 离线应用的 AppCache 清单内的文件  </li><li><strong>CachingResourceTransformer:</strong> 缓存其它 transfomer 的结果，作用同 <code>CachingResourceResolver</code>  </li><li><strong>CssLinkResourceTransformer:</strong> 处理 css 文件中的链接，为其加上版本号  </li><li><strong>ResourceTransformerSupport:</strong> 抽象类，自定义 transfomer 时继承  </li></ul><p>我们拿 CssLinkResourceTransformer 举例。 它会将 css 文件中的 @import 或 url() 函数中的资源路径自动转换为包含版本号的路径。</p><p>配置方法为：</p><figure class="highlight haxe"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">registry.addResourceHandler(<span class="string">"/static/**"</span>)</span><br><span class="line">                .addResourceLocations(<span class="string">"classpath:/static/"</span>)</span><br><span class="line">                .resourceChain(<span class="literal">false</span>)</span><br><span class="line">                .addResolver(<span class="keyword">new</span> <span class="type">VersionResourceResolver</span>().addContentVersionStrategy(<span class="string">"/**"</span>))</span><br><span class="line">                .addTransformer(<span class="keyword">new</span> <span class="type">CssLinkResourceTransformer</span>());</span><br></pre></td></tr></table></figure><p>当我们在 style.css 中通过 <code>@import &quot;style-other.css&quot;;</code> 导入了另一个 css 文件，则 transformer 会自动将该 style.css 内部的 css 文件路径地址转换为： <code>@import &quot;style-other-d41d8cd98f00b204e9800998ecf8427e.css&quot;</code></p><h2 id="4-6-Http-缓存"><a href="#4-6-Http-缓存" class="headerlink" title="4.6 Http 缓存"></a>4.6 Http 缓存</h2><p>为了避免客户端重复获取资源，<code>HTTP/1.1</code> 规范中定义了 <a href="https://developers.google.com/web/fundamentals/performance/optimizing-content-efficiency/http-caching?hl=zh-cn#cache-control" target="_blank" rel="external">Cache-Control</a> 头。几乎所有浏览器都实现了支持 <code>Cache-Control</code>。</p><p>配置方法如下：</p><figure class="highlight x86asm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">@Override</span><br><span class="line"><span class="meta">public</span> void addResourceHandlers(ResourceHandlerRegistry registry) &#123;</span><br><span class="line">   registry.addResourceHandler(<span class="string">"/static/**"</span>)</span><br><span class="line"><span class="meta">           .addResourceLocations</span>(<span class="string">"classpath:/static/"</span>)</span><br><span class="line"><span class="meta">           .setCacheControl</span>(CacheControl</span><br><span class="line"><span class="meta">                   .maxAge</span>(<span class="number">10</span>, TimeUnit.MINUTES)</span><br><span class="line"><span class="meta">                   .cachePrivate</span>())<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当请求 <code>/static/style.css</code> 时，返回的头信息中会多两条信息：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">Cache</span>-Control:<span class="keyword">max</span>-age=<span class="number">600</span>, <span class="keyword">private</span></span><br><span class="line"><span class="keyword">Last</span>-Modified:Sun, <span class="number">04</span> <span class="keyword">Oct</span> <span class="number">2016</span> <span class="number">15</span>:<span class="number">08</span>:<span class="number">22</span> GMT</span><br></pre></td></tr></table></figure><p>浏览器会将该信息连同资源储存起来，当再次请求该资源时，会取出 Last-Modified 并添加到在请求头 If-Modified-Since 中：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">If-Modified-Since</span><span class="selector-pseudo">:Sun</span>, 04 <span class="selector-tag">Oct</span> 2016 15<span class="selector-pseudo">:08</span><span class="selector-pseudo">:22</span> <span class="selector-tag">GMT</span></span><br></pre></td></tr></table></figure><p>Spring MVC 在收到请求，发现存在 <code>If-Modified-Since</code>，会提取出来该值，并与资源的修改时间比较，如果发现没有改变，则仅仅返回状态码 304，无需传递资源内容。浏览器收到状态码 304，明白资源从上次请求到现在未被改变，http 缓存依旧可用。</p><p>http 缓存的更多用法参见 <a href="https://developers.google.com/web/fundamentals/performance/optimizing-content-efficiency/http-caching?hl=zh-cn#cache-control" target="_blank" rel="external">这里</a>。</p><h1 id="5-使用-Spring-Boot-配置"><a href="#5-使用-Spring-Boot-配置" class="headerlink" title="5. 使用 Spring Boot 配置"></a>5. 使用 Spring Boot 配置</h1><p>众所周知，使用 Spring MVC 搭建 Web 服务，不仅要编写不少的代码或 XML 配置，如果开发人员使用不同的 IDE，还要配置这些 IDE 使其得以被正确运行。</p><p>为了解决这些问题，<a href="https://spring.io/" target="_blank" rel="external">spring.io</a> 平台提供了 Spring Boot。Spring Boot 采用 <a href="https://zh.wikipedia.org/wiki/%E7%BA%A6%E5%AE%9A%E4%BC%98%E4%BA%8E%E9%85%8D%E7%BD%AE" target="_blank" rel="external">约定优于配置</a> 的理念，在整合已有的 Spring 组件的同时，提供了大量的默认配置。得益于这些默认配置，使用 Spring Boot，只需要编写一个 pom.xml，再加上一个 java 类，就可以跑起来一个 web 服务，如果使用 groovy，一个类文件就能跑起来 web 服务。正是由于 spring boot 带来的这种便捷的特性，被广泛应用在微服务的场景中。</p><p>现在，Spring Boot 已经非常成熟了，最好的教程当然是<a href="http://projects.spring.io/spring-boot/" target="_blank" rel="external">官方文档</a>。</p><p>项目的创建可以为普通 maven 项目，当然还可以使用 spring.io 提供的 <a href="https://start.spring.io/" target="_blank" rel="external">在线创建 Spring Boot 项目</a> 的服务创建简项目或者。当然，也可以查看本文的示例代码。</p><p><strong>强烈推荐</strong> 看下 <a href="http://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/autoconfigure/web/WebMvcAutoConfiguration.html" target="_blank" rel="external">WebMvcAutoConfiguration</a> 这个类，它为 Spring Boot 提供了大量的 Web 服务的默认配置。这些配置包括但不局限于：设置了主页、webjars配置、静态资源位置等。这些配置对于我们使用配置 Web 服务很有借鉴意义。</p><p><strong>ps: 想要使用默认配置，无需使用 @EnaleWebMvc 注解。使用了 @EnableWebMvc 注解后 WebMvcAutoConfiguration 提供的默认配置会失效，必须提供全部配置。</strong></p><p>最后，我们使用 spring boot 提供的编写配置文件的方式，实现上面使用代码才能完成的功能。</p><figure class="highlight routeros"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># application.properties</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置静态资源的存放地址</span></span><br><span class="line">spring.resources.<span class="attribute">static-locations</span>=classpath:/resources </span><br><span class="line"></span><br><span class="line"><span class="comment"># 开启 chain cache</span></span><br><span class="line">spring.resources.chain.<span class="attribute">cache</span>=<span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 开启 gzip</span></span><br><span class="line">spring.resources.chain.<span class="attribute">gzipped</span>=<span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 指定版本号</span></span><br><span class="line">spring.resources.chain.strategy.fixed.<span class="attribute">enabled</span>=<span class="literal">true</span></span><br><span class="line">spring.resources.chain.strategy.fixed.<span class="attribute">paths</span>=/static  </span><br><span class="line">spring.resources.chain.strategy.fixed.<span class="attribute">version</span>=1.0.0</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用 MD5 作为版本号</span></span><br><span class="line">spring.resources.chain.strategy.content.<span class="attribute">enable</span>=<span class="literal">true</span></span><br><span class="line">spring.resources.chain.strategy.content.<span class="attribute">paths</span>=/**</span><br><span class="line"></span><br><span class="line"><span class="comment"># http 缓存过期时间</span></span><br><span class="line">spring.resources.<span class="attribute">cachePeriod</span>=60</span><br></pre></td></tr></table></figure><p>最后介绍一下如何查看这些配置的技巧：</p><p>通过查看 <a href="https://github.com/spring-projects/spring-boot/blob/master/spring-boot-autoconfigure/src/main/java/org/springframework/boot/autoconfigure/web/ResourceProperties.java" target="_blank" rel="external">ResourceProperties</a> 这个类可以看到，该类顶部有一个注解 <code>@ConfigurationProperties(prefix = &quot;spring.resources&quot;, ignoreUnknownFields = false)</code>。</p><p>ConfigurationProperties 是用来注入值的，<code>prefix = &quot;spring.resources&quot;</code> 表示前缀。比如我们配置文件中的 <code>spring.resources.static-locations=classpath:/resources</code> 这个配置，去掉 <code>spring.resources</code> 这个前缀，剩下的为 <code>static-locations</code> ，则它的值 <code>classpath:/resources</code> 会被注入到 ConfigurationProperties 类的 staticLocations 成员变量中。通过这种方法，我们就能通过编写配置文件改变类的状态而无需编写代码。当然，如何使用这些配置的关键还是要知道这些成员变量的作用。</p><h1 id="6-总结"><a href="#6-总结" class="headerlink" title="6. 总结"></a>6. 总结</h1><p>本文从一个新的技术点 webjars 出发，探讨了 Spring MVC 对静态资源的处理，紧接着又了解了 Spring Boot 的配置技巧。</p><p>示例代码：<a href="https://code.csdn.net/tanheaishui/webjartest/repository/archive?ref=master" target="_blank" rel="external">下载</a></p><h1 id="7-参考"><a href="#7-参考" class="headerlink" title="7. 参考"></a>7. 参考</h1><p><a href="https://developers.google.com/web/fundamentals/performance/optimizing-content-efficiency/http-caching?hl=zh-cn#cache-control" target="_blank" rel="external">https://developers.google.com/web/fundamentals/performance/optimizing-content-efficiency/http-caching?hl=zh-cn#cache-control</a><br><a href="http://docs.spring.io/spring/docs/current/spring-framework-reference/htmlsingle/#mvc-config-static-resources" target="_blank" rel="external">http://docs.spring.io/spring/docs/current/spring-framework-reference/htmlsingle/#mvc-config-static-resources</a><br><a href="http://qiita.com/kazuki43zoo/items/e12a72d4ac4de418ee37" target="_blank" rel="external">http://qiita.com/kazuki43zoo/items/e12a72d4ac4de418ee37</a><br><a href="http://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/#boot-features-spring-mvc-static-content" target="_blank" rel="external">http://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/#boot-features-spring-mvc-static-content</a></p>]]></content>
    
    <summary type="html">
    
      &lt;h1 id=&quot;1-背景&quot;&gt;&lt;a href=&quot;#1-背景&quot; class=&quot;headerlink&quot; title=&quot;1. 背景&quot;&gt;&lt;/a&gt;1. 背景&lt;/h1&gt;&lt;p&gt;前一段时间，WebIDE 开源的过程中，无意间接触到 &lt;a href=&quot;http://www.webjars.org/&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;webjars&lt;/a&gt;，觉得比较有趣，于是研究并整理了一下。&lt;/p&gt;
&lt;p&gt;webjars 是将前端的库（比如 jQuery）打包成 Jar 文件，然后使用基于 JVM 的包管理器（比如 Maven、Gradle 等）管理前端依赖的方案。&lt;/p&gt;
&lt;p&gt;webjars 的效果非常神奇。对于其用法，我们可以在 maven 项目中添加下面的依赖：&lt;/p&gt;
&lt;figure class=&quot;highlight xml&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;5&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;dependency&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;groupId&lt;/span&gt;&amp;gt;&lt;/span&gt;org.webjars&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;groupId&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;artifactId&lt;/span&gt;&amp;gt;&lt;/span&gt;jquery&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;artifactId&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;tag&quot;&gt;&amp;lt;&lt;span class=&quot;name&quot;&gt;version&lt;/span&gt;&amp;gt;&lt;/span&gt;3.1.0&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;version&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;tag&quot;&gt;&amp;lt;/&lt;span class=&quot;name&quot;&gt;dependency&lt;/span&gt;&amp;gt;&lt;/span&gt;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;然后通过请求 &lt;code&gt;http://localhost:8080/webjars/jquery/3.1.0/jquery.js&lt;/code&gt; 即可正确访问到 jquery 文件。&lt;/p&gt;
    
    </summary>
    
      <category term="spring" scheme="http://yoursite.com/categories/spring/"/>
    
    
      <category term="spring" scheme="http://yoursite.com/tags/spring/"/>
    
  </entry>
  
  <entry>
    <title>使用原理视角看 Git</title>
    <link href="http://yoursite.com/2016/07/05/git-implement/"/>
    <id>http://yoursite.com/2016/07/05/git-implement/</id>
    <published>2016-07-05T01:12:00.000Z</published>
    <updated>2017-09-27T02:14:20.923Z</updated>
    
    <content type="html"><![CDATA[<h1 id="1-开始"><a href="#1-开始" class="headerlink" title="1. 开始"></a>1. 开始</h1><p>大家下午好，欢迎大家来到 Coding 技术小馆，我叫谭贺贺，目前我在Coding 主要负责 WebIDE 与 Codeinsight 的开发。我今天带来的主要内容是 git 的原理与使用。</p><p>谈起 git，大家的第一印象无非是和 svn 一样的版本控制系统，但其实，他们有着非常大的不同，至少 svn 没有像git一样这么多的玩法。下面我举几个例子，简略的说一下。</p><h2 id="1-1-搭建博客"><a href="#1-1-搭建博客" class="headerlink" title="1.1 搭建博客"></a>1.1 搭建博客</h2><p>阮一峰将写 blog 的人分成三个阶段</p><p>使用免费空间，比如 CSDN、博客园。<br>发现免费空间限制太多，于是自己购买域名和空间，搭建独立博客。<br>独立博客管理太麻烦，最好在保留控制权的前提下，让别人来管，自己负责写文章。</p><p>其实第三种阶段指的就是使用 Pages 服务。很多公司比如 Coding、Github 等代码托管平台都推出了 Pages 服务，可以用来搭建个人博客。Pages 服务不需要复杂的配置，就可以完成博客的搭建。</p><p>在使用 Pages 的过程中，通过使用标记语言（Markdown）完成博客的编写，推送到服务器上，就可以看到新发布的博客了。</p><p>不需要管理服务器，降低了搭建博客的门槛，同时又保持了用户对博客的高度定制权。</p><h2 id="1-2-写书"><a href="#1-2-写书" class="headerlink" title="1.2 写书"></a>1.2 写书</h2><p>很多牛人喜欢写博客，博客写多了，然后汇集起来就出了本书。比如 Matrix67《思考的乐趣》、阮一峰《如何变得有思想》就是这样的例子。</p><p>其实出书距离我们也并不遥远，为什么？因为有 gitbook 这类服务。</p><p>对于 git + Pages 服务的用户，gitbook 很容易上手，因为使用 gitbook 就是使用 git 与 markdown。<br>你完全可以将你 markdown 的博客 copy，汇集起来，形成一本书籍。内容的排版 gitbook 会帮你做，我们只负责内容就可以了。编写好内容，我们就能立刻获得  html、pdf、epub、mobi 四个版本的电子书。这是 html 版的预览：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/b7ce6489-9af8-4526-af2e-ef8f32dbf56c.png" alt="图片"></p><p>在 gitbook 上有 explore 频道，上面列出了所有公开的书籍（当然也可以直接搜索）。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/ea39dad5-5a68-41ea-b457-b1f38b2a0aee.png" alt="图片"></p><p>实际上，除了写书，还可以连同其他人一起进行外文资料的翻译，举个例子《The Swift Programming Language》中文版，将英文版分成几个部分，然后在开源项目中由参与者认领翻译，每个人贡献一份自己的力量，完成了这样以非常快的相应速度跟随官方文档更新的操作。如果你喜欢的一门语言，或者技术，中文资料缺乏，大家可以发起这样的活动，完成外文资料的翻译。</p><h2 id="1-3-人才招聘"><a href="#1-3-人才招聘" class="headerlink" title="1.3 人才招聘"></a>1.3 人才招聘</h2><p>人才招聘这一块，至今还并没有形成一定的规模。但仍旧有很多的公司选择在代码托管平台上（比如 Coding、Github）上寻找中意的开发者。</p><p>有一些开发者看准了这一块，专门开发了这样的网站，比如 githuber.cn、github-awards.com。</p><p>拿 githuber 举例，该网站主要提供两个功能，第一个是星榜，说白了将所有所有用户按照语言分类，然后根据粉丝数（star）排序。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/f96717cb-b48b-4123-bc46-1f0046bc37d4.png" alt="图片"></p><p>我们可以很容易的看到排行榜上前几位的用户，他们的开源项目，这在一定程度上能代表这门语言的发展趋势。比如我对java比较感兴趣，然后我看了一下前十名，发现大部分都是 android 开发，由此可见android开发的火爆程度。</p><p>当然你也可以看到你的排名，会让你有打怪升级的快感。</p><p>第二个功能是搜索，输入筛选条件，搜搜程序员！</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/e34a0d9d-5158-44b6-adb2-7bd2ebe3451a.png" alt="图片"></p><h2 id="1-4-WebIDE"><a href="#1-4-WebIDE" class="headerlink" title="1.4 WebIDE"></a>1.4 WebIDE</h2><p>Coding WebIDE 是 Coding 自主研发的在线集成开发环境 (IDE)。只要你的项目在代码托管平台存放，就可以导入到 WebIDE。之后就可以在线开发。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/f495bedb-cc65-4355-a2b8-5bdac5860eda.png" alt="图片"></p><p>WebIDE 还提供 WebTerminal 功能，用户可以远程操作Docker容器，自由安装偏好的软件包、方便折腾。</p><p>看起来是不是还挺好玩的，如果想把这些都玩转，git 是肯定要好好学的。接下来，我们就看一下 git 的基本原理。</p><h1 id="2-Git-原理"><a href="#2-Git-原理" class="headerlink" title="2. Git 原理"></a>2. Git 原理</h1><p>我们可以现在想一下，如果我们自己来设计，应该怎么设计。</p><p>传统的设计方案我们可以简单的分成两块：工作目录，远程仓库。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/47074771-c5ac-414f-a237-ea1e791991bd.png" alt="图片"></p><p>但是作为一个目标明确的分布式版本控制系统，首先要做的就是添加一个本地仓库。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/3734cb87-6507-40ce-a8ac-428b48d95d04.png" alt="图片"></p><p>接着我们选择在工作目录与远程仓库中间加一个缓冲区域，叫做暂存区。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/fc5a194b-8ad0-4fd6-8c7a-fb26062bd3fb.png" alt="图片"></p><p>加入暂存区的原因有以下几点：</p><ol><li>为了能够实现部分提交</li><li>为了不再工作区创建状态文件、会污染工作区。</li><li>暂存区记录文件的修改时间等信息，提高文件比较的效率。</li></ol><p>至此就我们本地而言有三个重要的区域：工作区、暂存区、本地仓库。</p><p>接下来我们想一下本地仓库是如何存放项目历史版本。</p><h2 id="2-1-快照"><a href="#2-1-快照" class="headerlink" title="2.1 快照"></a>2.1 快照</h2><p> <img src="https://dn-coding-net-production-pp.qbox.me/f1e4cbe9-1f7d-4826-8a61-1f7d065e43ce.png" alt="图片"></p><p>这是项目的三个版本，版本1中有两个文件A和B，然后修改了A，变成了A1，形成了版本2，接着又修改了B变为B1，形成了版本3。</p><p>如果我们把项目的每个版本都保存到本地仓库，需要保存至少6个文件，而实际上，只有4个不同的文件，A、A1、B、B1。为了节省存储的空间，我们要像一个方法将同样的文件只需要保存一份。这就引入了Sha-1算法。</p><p>可以使用git命令计算文件的 sha-1 值。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">'test content'</span> | git <span class="built_in">hash</span>-object --stdin</span><br><span class="line">d670460b4b4aece5915caf5c68d12f560a9fe3e4</span><br></pre></td></tr></table></figure><p>SHA-1将文件中的内容通过通过计算生成一个 40 位长度的hash值。</p><p>Sha-1的非常有特点：</p><ul><li>由文件内容计算出的hash值</li><li>hash值相同，文件内容相同</li></ul><p>对于上图中的内容，无论我们执行多少次，都会得到相同的结果。因此，文件的sha-1值是可以作为文件的唯一 id 。同时，它还有一个额外的功能，校验文件完整性。</p><p>有了 sha-1 的帮助，我们可以对项目版本的存储方式做一下调整。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/ededf2f2-9c29-4234-92f6-5c89d12bce3d.png" alt="图片"></p><h3 id="2-1-1-数据库中存储的数据内容"><a href="#2-1-1-数据库中存储的数据内容" class="headerlink" title="2.1.1 数据库中存储的数据内容"></a>2.1.1 数据库中存储的数据内容</h3><p>实际上，现在就与git实际存储的结构一致了。我们可以预览一下实际存储在 .git 下的文件。</p><p><img src="https://dn-coding-net-production-pp.qbox.me/e494e331-4bde-4bc0-850f-678121f829c9.png" alt="图片"></p><p>我们可以看到，在 objects 目录下，存放了很多文件，他们都使用 sha-1 的前两位创建了文件夹，剩下的38位为文件名。我们先称呼这些文件为 obj 文件。</p><p>对于这么多的 obj 文件，就保存了我们代码提交的所有记录。对于这些 obj 文件，其实分为四种类型，分别是 blob、tree、commit、tag。接下来，我们分别来看一下。</p><ol><li><p>blob</p><p> 首先  A、A1、B、B1 就是 blob 类型的 obj。</p><p> blob: 用来存放项目文件的内容，但是不包括文件的路径、名字、格式等其它描述信息。项目的任意文件的任意版本都是以blob的形式存放的。            </p></li><li><p>tree</p><p> tree 用来表示目录。我们知道项目就是一个目录，目录中有文件、有子目录。因此 tree 中有 blob、子tree，且都是使用 sha-1值引用的。这是与目录对应的。从顶层的 tree 纵览整个树状的结构，叶子结点就是blob，表示文件的内容，非叶子结点表示项目的目录，顶层的 tree 对象就代表了当前项目的快照。</p></li><li><p>commit</p><p> commit: 表示一次提交，有parent字段，用来引用父提交。指向了一个顶层 tree，表示了项目的快照，还有一些其它的信息，比如上一个提交，committer、author、message 等信息。</p></li></ol><h2 id="2-2-暂存区"><a href="#2-2-暂存区" class="headerlink" title="2.2 暂存区"></a>2.2 暂存区</h2><p>暂存区是一个文件，路径为： <code>.git/index</code></p><p><img src="https://dn-coding-net-production-pp.qbox.me/a2897509-382d-4a2a-9d94-4c4d8e8a4051.bmp" alt="图片"></p><p>它是一个二进制文件，但是我们可以使用命令来查看其中的内容。<br>这里我们关注第二列和第四列就可以了，第四列是文件名，第二列指的是文件的blob。这个blob存放了文件暂存时的内容。</p><p>第二列就是sha-1 hash值，相当于内容的外键，指向了实际存储文件内容的blob。第三列是文件的冲突状态，这个后面会讲，第四列是文件的路径名。</p><p>我们操作暂存区的场景是这样的，每当编辑好一个或几个文件后，把它加入到暂存区，然后接着修改其他文件，改好后放入暂存区，循环反复。直到修改完毕，最后使用 commit 命令，将暂存区的内容永久保存到本地仓库。</p><p>这个过程其实就是构建项目快照的过程，当我们提交时，git 会使用暂存区的这些信息生成tree对象，也就是项目快照，永久保存到数据库中。因此也可以说暂存区是用来构建项目快照的区域。</p><h2 id="2-3-文件状态"><a href="#2-3-文件状态" class="headerlink" title="2.3 文件状态"></a>2.3 文件状态</h2><p>有了工作区、暂存区、本地仓库，就可以来定义文件的状态了。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/700dae41-aa3a-4b95-9afb-3fe7291c53d1.png" alt="图片"></p><p>文件的状态可以分为两类。一类是暂存区与本地仓库比较得出的状态，另一类是工作区与暂存区比较得出的状态。为什么要分成两类的愿意也很简单，因为第一类状态在提交时，会直接写入本地仓库。而第二种则不会。一个文件可以同时拥有两种状态。</p><p>比如一个文件可能既有上面的 modified 状态，又有下面 modified 状态，但其实他们表示了不同的状态，git 会使用绿色和红色把这两中 modified 状态区分开来。</p><h2 id="2-4-分支"><a href="#2-4-分支" class="headerlink" title="2.4 分支"></a>2.4 分支</h2><p>接下来，看一个很重要的概念，分支。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/876aff53-1fd9-44b2-bfe4-bc97f35cd192.png" alt="图片"></p><p>分支的目的是让我们可以并行的进行开发。比如我们当前正在开发功能，但是需要修复一个紧急bug，我们不可能在这个项目正在修改的状态下修复 bug，因为这样会引入更多的bug。</p><p>有了分支的概念，我们就可以新建一个分支，修复 bug，使新功能与 bug 修复同步进行。</p><p>分支的实现其实很简单，我们可以先看一下 .git/HEAD 文件，它保存了当前的分支。</p><figure class="highlight excel"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cat .git/HEAD</span><br><span class="line">=&gt;r<span class="symbol">ef:</span> refs/heads/master</span><br></pre></td></tr></table></figure><p>其实这个 ref 表示的就是一个分支，它也是一个文件，我们可以继续看一下这个文件的内容：</p><figure class="highlight excel"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cat .git/refs/heads/master</span><br><span class="line">=&gt; <span class="number">2</span>b388d2c1c20998b6233ff47596b0c87ed3ed8f8</span><br></pre></td></tr></table></figure><p>可以看到分支存储了一个 object，我们可以使用 cat-file 命令继续查看该 object 的内容。</p><figure class="highlight excel"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">git cat-file -p <span class="number">2</span>b388d2c1c20998b6233ff47596b0c87ed3ed8f8</span><br><span class="line">=&gt; tree <span class="number">15</span>f880be0567a8844291459f90e9d0004743c8d9</span><br><span class="line">=&gt; parent <span class="number">3</span>d885a272478d0080f6d22018480b2e83ec2c591</span><br><span class="line">=&gt; author Hehe <span class="built_in">Tan</span> &lt;xiayule148@gmail.com&gt; <span class="number">1460971725</span> +<span class="number">0800</span></span><br><span class="line">=&gt; committer Hehe <span class="built_in">Tan</span> &lt;xiayule148@gmail.com&gt; <span class="number">1460971725</span> +<span class="number">0800</span></span><br><span class="line">=&gt;</span><br><span class="line">=&gt; add branch paramter for rebase</span><br></pre></td></tr></table></figure><p>从上面的内容，我们知道了分支指向了一次提交。为什么分支指向一个提交的原因，其实也是git中的分支为什么这么轻量的答案。</p><p>因为分支就是指向了一个 commit 的指针，当我们提交新的commit，这个分支的指向只需要跟着更新就可以了，而创建分支仅仅是创建一个指针。</p><h1 id="3-高层命令"><a href="#3-高层命令" class="headerlink" title="3. 高层命令"></a>3. 高层命令</h1><p>在 git 中分为两种类型的命令，一种是完成底层工作的工具集，称为底层命令，另一种是对用户更友好的高层命令。一条高层命令，往往是由多条底层命令组成的。</p><p>不知道的人可能一听高层感觉很厉害的样子，其实就是指的是那些我们最常使用的git命令。</p><h2 id="3-1-Add-amp-Commit"><a href="#3-1-Add-amp-Commit" class="headerlink" title="3.1 Add &amp; Commit"></a>3.1 Add &amp; Commit</h2><p>add 和 commit 应该可以说是我们使用频率最高的高层命令了。</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">touch README.md</span><br><span class="line">git add README.md</span><br><span class="line">git commit -m <span class="string">"add readme”</span></span><br></pre></td></tr></table></figure><p>touch 指的是创建一个文件，代表了我们对项目文件内容的修改，add 操作是将修改保存到暂存区，commit 是将暂存区的内容永久保存到本地仓库。</p><p>每当将修改的文件加入到暂存区，git 都会根据文件的内容计算出 sha-1，并将内容转换成 blob，写入数据库。然后使用 sha-1 值更新该列表中的文件项。在暂存区的文件列表中，每一个文件名，都会对应一个sha-1值，用于指向文件的实际内容。最后提交的那一刻，git会将这个列表信息转换为项目的快照，也就是 tree 对象。写入数据库，并再构建一个commit对象，写入数据库。然后更新分支指向。</p><h2 id="3-2-Conflicts-amp-Merge-amp-Rebase"><a href="#3-2-Conflicts-amp-Merge-amp-Rebase" class="headerlink" title="3.2 Conflicts &amp; Merge &amp; Rebase"></a>3.2 Conflicts &amp; Merge &amp; Rebase</h2><h2 id="3-2-1-Conflicts"><a href="#3-2-1-Conflicts" class="headerlink" title="3.2.1 Conflicts"></a>3.2.1 Conflicts</h2><p>git 中的分支十分轻量，因此我们在使用git的时候会频繁的用到分支。不可不免的需要将新创建的分支合并。</p><p>在 git 中合并分支有两种选择：merge 和 rebase。但是，无论哪一种，都有可能产生冲突。因此我们先来看一下冲突的产生。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/b39e3a39-0091-4265-ba6d-73ef0fd457b2.png" alt="图片"></p><p>图上的情况，并不是移动分支指针就能解决问题的，它需要一种合并策略。首先，我们需要明确的是谁和谁的合并，是 2，3 与 4，5，6的合并吗？说到分支，我们总会联想到线，就会认为是线的合并。其实不是的，真实合并的是 3 和 6。因为每一次提交都包含了项目完整的快照，即合并只是 tree 与 tree 的合并。</p><p>我们可以先想一个简单的算法。用来比较3和6。但是我们还需要一个比较的标准，如果只是3和6比较，那么3与6相比，添加了一个文件，也可以说成是6与3比删除了一个文件，这无法确切表示当前的冲突状态。因此我们选取他们的两个分支的分歧点（merge base）作为参考点，进行比较。</p><p>比较时，相对于 merge base（提交1）进行比较。</p><p>首先把1、3、6中所有的文件做一个列表，然后依次遍历这个列表中的文件。现在我们拿列表中的一个文件进行举例，把在提交1、3、6中的该文件分别称为版本1、版本3、版本6。</p><ol><li>版本1、版本3、版本6的 sha-1 值完全相同，这种情况表明没有冲突</li><li>版本3或6至少一个与版本1状态相同（指的是sha-1值相同或都不存在），这种情况可以自动合并。比如1中存在一个文件，3对该文件进行修改，而6中删除了这个文件，则以6为准就可以了</li><li>版本3或版本6都与版本1的状态不同，情况复杂一些，自动合并策略很难生效，需要手动解决。我们来看一下这种状态的定义。</li></ol><p>冲突状态定义：</p><ul><li><strong>1 and 3:</strong> DELETED_BY_THEM;</li><li><strong>1 and 6:</strong> DELETED_BY_US;</li><li><strong>3 and 6:</strong> BOTH_ADDED;</li><li><strong>1 and 3 and 6</strong>: BOTH_MODIFIED</li></ul><p>我们拿第一种情况举例，文件有两种状态 1 和 3，1 表示该文件存在于 commit 1（也就是MERGE_BASE），3 表示该文件在 commit 3 （master 分支）中被修改了，没有 6，也就是该文件在 commit 6（feature 分支）被删除了，总结来说这种状态就是 DELETED_BY_THEM。</p><p>可以再看一下第四种情况，文件有三种状态 1、3、6，1 表示 commit 1（MERGE_BASE）中存在，3 表示 commit 3（master 分支）进行了修改，6 表示（feature 分支）也进行了修改，总结来说就是 BOTH_MODIFIED（双方修改）。</p><p>遇到不可自动合并冲突时，git会将这些状态写入到暂存区。与我们讨论不同的是，git使用1，2，3标记文件，1表示文件的base版本，2表示当前的分支的版本，3表示要合并分支的版本。</p><h3 id="3-2-2-Merge"><a href="#3-2-2-Merge" class="headerlink" title="3.2.2 Merge"></a>3.2.2 Merge</h3><p>在解决完冲突后，我们可以将修改的内容提交为一个新的提交。这就是 merge。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/d8ae5977-a10d-49c2-a43c-8d633a5935a0.png" alt="图片"></p><p>merge 之后仍可以做出新的提交。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/707cff0a-95d7-4420-a6da-607281d71cd7.png" alt="图片"></p><p>可以看到 merge 是一种不修改分支历史提交记录的方式，这也是我们常用的方式。但是这种方式在某些情况下使用    起来不太方便，比如当我们创建了 pr、mr 或者 将修改补丁发送给管理者，管理者在合并操作中产生了冲突，还需要去解决冲突，这无疑增加了他人的负担。</p><p>使用 rebase 可以解决这种问题。</p><h3 id="3-2-3-Rebase"><a href="#3-2-3-Rebase" class="headerlink" title="3.2.3 Rebase"></a>3.2.3 Rebase</h3><p>假设我们的分支结构如下：</p><p><img src="https://dn-coding-net-production-pp.qbox.me/b39e3a39-0091-4265-ba6d-73ef0fd457b2.png" alt="图片"></p><p>rebase 会把从 Merge Base 以来的所有提交，以补丁的形式一个一个重新达到目标分支上。这使得目标分支合并该分支的时候会直接 Fast Forward，即不会产生任何冲突。提交历史是一条线，这对强迫症患者可谓是一大福音。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/f7ed7492-afd8-4c69-b5c1-3034161fbb2c.png" alt="图片"></p><p>如果我们想要看 rebase 实际上做了什么，有一个方法，那就是用“慢镜头”来看rebase的整个操作过程。rebase 提供了交互式选项(参数 -i)，我们可以针对每一个patch，选择你要进行的操作。</p><p>通过这个交互式选项，我们可以”单步调试”rebase操作。</p><p>经过测试，其实 rebase 主要在 .git/rebase-merge 下生成了两个文件，分别为 git-rebase-todo 和 done 文件，这两个文件的作用光看名字就可以看得出来。git-rebase-todo 存放了 rebase 将要操作的 commit。而 done 存放正在操作或已经操作完毕的 commit。比如我们这里，git-rebase-todo 存放了 4、5、6，三个提交。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/02a5f8b9-72a8-494c-aeb7-50a1637d4b3b.png" alt="图片"></p><p>首先 git 将 sha-1 为 4 的 commit 放入 done。表示正在操作 4，然后将 4 以补丁的形式打到 3 上，形成了新的提交 4’。这一步是可能产生冲突的，如果有冲突，需要解决完冲突之后才能继续操作。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/c401cb72-47fb-4ee3-8a02-e6b7d8817eed.png" alt="图片"></p><p>接着讲 sha-1 为 5 的提交放入 done 文件，然后将 5 以补丁的形式打到 4’ 上，形成 5’。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/832e68e8-326a-4e60-a628-fd8661f36430.png" alt="图片"></p><p>再接着将 sha-1 为 6 的提交放入 done 文件，然后将 6 以补丁的形式打到 5’ 上，形成 6’。最后移动分支指针，使其指向最新的提交 6’ 上。这就完成了 rebase 的操作。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/951cc638-7fd3-4e60-9452-eb17e5e12d89.png" alt="图片"></p><p>我们看一下真实的 rebase 文件。</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">pick e0f56d9 update gitignore</span><br><span class="line">pick e370289 <span class="keyword">add</span><span class="bash"> a</span></span><br><span class="line"><span class="bash"></span></span><br><span class="line"><span class="bash"><span class="comment"># Commands:</span></span></span><br><span class="line"><span class="bash"><span class="comment"># p, pick = use commit</span></span></span><br><span class="line"><span class="bash"><span class="comment"># r, reword = use commit, but edit the commit message</span></span></span><br><span class="line"><span class="bash"><span class="comment"># e, edit = use commit, but stop for amending</span></span></span><br><span class="line"><span class="bash"><span class="comment"># s, squash = use commit, but meld into previous commit</span></span></span><br><span class="line"><span class="bash"><span class="comment"># f, fixup = like "squash", but discard this commit's log message</span></span></span><br><span class="line"><span class="bash"><span class="comment"># x, exec = run command (the rest of the line) using shell</span></span></span><br><span class="line"><span class="bash"><span class="comment"># d, drop = remove commit</span></span></span><br></pre></td></tr></table></figure><p>该文件一共有三列，第一列表示要进行的操作，所有可以进行的操作，在下面注释里都列了出来，比如 pick 表示使用该提交，reword 表示使用该提交，但修改其提交的 message，edit 表示使用该提交，但是要对该提交进行一些修改，其它的就不一一说了。</p><p>而 done 文件的形式如下，和 git-rebase-todo 是一样的：</p><figure class="highlight dockerfile"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">pick e0f56d9 update gitignore</span><br><span class="line">pick e370289 <span class="keyword">add</span><span class="bash"> a</span></span><br></pre></td></tr></table></figure><p>从刚才的图中，我们就可以看到 rebase 的一个缺点，那就是修改了分支的历史提交。如果已经将分支推送到了远程仓库，会导致无法将修改后的分支推送上去，必须使用 -f 参数（force）强行推送。</p><p>所以使用 rebase 最好不要在公共分支上进行操作。</p><h2 id="3-3-Checkout、Revert、Reset"><a href="#3-3-Checkout、Revert、Reset" class="headerlink" title="3.3 Checkout、Revert、Reset"></a>3.3 Checkout、Revert、Reset</h2><h3 id="3-3-1-Checkout"><a href="#3-3-1-Checkout" class="headerlink" title="3.3.1 Checkout"></a>3.3.1 Checkout</h3><p>对于 checkout，我们一般不会陌生。因为使用它的频率非常高，经常用来切换分支、或者切换到某一次提交。</p><p>这里我们以切换分支为例，从 git 的工作区、暂存区、本地仓库分别来看 checkout 所做的事情。Checkout 前的状态如下：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/76b4be67-1cef-4b00-9f9c-604ce9b8489a.png" alt="图片"></p><p>首先 checkout 找到目标提交（commit），目标提交中的快照也就是 tree 对象就是我们要检出的项目版本。<br>checkout 首先根据tree生成暂存区的内容，再根据 tree 与其包含的 blob 转换成我们的项目文件。然后修改 HEAD 的指向，表示切换分支。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/8a364874-b99f-409c-b068-a27569f0bb35.png" alt="图片"></p><p>可以看到 checkout 并没有修改提交的历史记录。只是将对应版本的项目内容提取出来。</p><h3 id="3-3-2-Revert"><a href="#3-3-2-Revert" class="headerlink" title="3.3.2 Revert"></a>3.3.2 Revert</h3><p>如果我们想要用一个用一个反向提交恢复项目的某个版本，那就需要 revert 来协助我们完成了。什么是反向提交呢，就是旧版本添加了的内容，要在新版本中删除，旧版本中删除了的内容，要在新版本中添加。这在分支已经推送到远程仓库的情境下非常有用。</p><p>Revert 之前：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/091cf782-a007-47dd-aa80-c424427d5bfc.png" alt="图片"></p><p>revert 也不会修改历史提交记录，实际的操作相当于是检出目标提交的项目快照到工作区与暂存区，然后用一个新的提交完成版本的“回退”。</p><p>Revert 之后：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/178cccb3-2710-4af1-bb60-0dde3e150626.png" alt="图片"></p><h3 id="Reset"><a href="#Reset" class="headerlink" title="Reset"></a>Reset</h3><p>reset 操作与 revert 很像，用来在当前分支进行版本的“回退”，不同的是，reset 是会修改历史提交记录的。</p><p>reset 常用的选项有三个，分别是 —soft, —mixed, —hard。他们的作用域依次增大。</p><p>我们分别来看。</p><p>soft 会仅仅修改分支指向。而不修改工作区与暂存区的内容，我们可以接着做一次提交，形成一个新的 commit。这在我们撤销临时提交的场景下显得比较有用。</p><p>使用 reset –soft 前：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/a2b153ad-459f-4ba0-8dc6-09cc333a7703.png" alt="图片"></p><p>使用 reset –soft 后:</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/30405590-b595-4f69-9b6a-c96f1cb1feea.png" alt="图片"></p><p>mixed 比 soft 的作用域多了一个 暂存区。实际上 mixed 选项与 soft 只差了一个 add 操作。</p><p>使用 reset –mixed 前：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/f80ac7b1-dff2-4dbe-aca8-6e1e7c7de921.png" alt="图片"></p><p>使用 reset –mixed 后：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/94342f08-f101-41f8-9396-8b3de51e2c9d.png" alt="图片"></p><p>hard 会作用域又比 mixed 多了一个 工作区。</p><p>使用 reset –hard 前：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/961691a6-c075-4bb8-8dc1-5efe70813b53.png" alt="图片"></p><p>使用 reset –hard 后：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/f8f55209-7bba-470b-a7c6-88c5f9594fe3.png" alt="图片"></p><p>hard 选项会导致工作区内容“丢失”。</p><p>在使用 hard 选项时，一定要确保知道自己在做什么，不要在迷糊的时候使用这条选项。如果真的误操作了，也不要慌，因为只要 git 一般不会主动删除本地仓库中的内容，根据你丢失的情况，可以进行找回，比如在丢失后可以使用 git reset –hard ORIG_HEAD 立即恢复，或者使用 reflog 命令查看之前分支的引用。</p><h2 id="3-4-stash"><a href="#3-4-stash" class="headerlink" title="3.4 stash"></a>3.4 stash</h2><p>有时，我们在一个分支上做了一些工作，修改了很多代码，而这时需要切换到另一个分支干点别的事。但又不想将只做了一半的工作提交。在曾经这样做过，将当前的修改做一次提交，message 填写 half of work，然后切换另一个分支去做工作，完成工作后，切换回来使用 reset —soft 或者是 commit amend。</p><p>git 为了帮我们解决这种需求，提供了 stash 命令。</p><p>stash 将工作区与暂存区中的内容做一个提交，保存起来，然后使用reset hard选项恢复工作区与暂存区内容。我们可以随时使用 stash apply 将修改应用回来。</p><p>stash 实现思路将我们的修改提交到本地仓库，使用特殊的分支指针（.git/refs/stash）引用该提交，然后在恢复的时候，将该提交恢复即可。我们可以更进一步，看看 stash 做的提交是什么样的结构。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/775266cc-d649-404e-9395-0766bb452579.png" alt="图片"></p><p>如图所示，如果我们提供了 —include-untracked 选项，git 会将 untracked 文件做一个提交，但是该提交是一个游离的状态，接着将暂存区的内容做一个提交。最后将工作区的修改做一个提交，并以untracked 的提交、暂存区 的提交、基础提交为父提交。</p><p>搞这么复杂，是为了提供更灵活地选项，我们可以选择性的恢复其中的内容。比如恢复 stash 时，可以选择是否重建 index，即与 stash 操作时完全一致的状态。</p><h2 id="3-5-bisect"><a href="#3-5-bisect" class="headerlink" title="3.5 bisect"></a>3.5 bisect</h2><p>最后要讲到一个曾经把我从“火坑”中救出来的功能。</p><p>项目发布到线上的项目出现了bug，而经过排查，却找不到问 bug 的源头。我们还有一种方法，那就是先找到上一次好的版本，从上一次到本次之间的所有提交依次尝试，一一排查。直到找到出现问题的那一次提交，然后分析 bug 原因。</p><p>git 为我们想到了这样的场景，同样是刚才的思路，但是使用二分法进行查找。这就是 bisect 命令。</p><p>使用该命令很简单，</p><figure class="highlight armasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="symbol">git</span> <span class="keyword">bisect </span>start</span><br><span class="line"><span class="symbol">git</span> <span class="keyword">bisect </span><span class="keyword">bad </span>HEAD</span><br><span class="line"><span class="symbol">git</span> <span class="keyword">bisect </span>good <span class="built_in">v4</span>.<span class="number">1</span></span><br></pre></td></tr></table></figure><p>git 会计算中间的一个提交，然后我们进行测试。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/1175a6e4-0a52-40b1-a451-23f8eaa123ca.png" alt="图片"></p><p>根据测试结果，使用  git bisect good or bad 进行标记，git 会自动切换到下一个提交。不断的重复这个步骤，直到找到最初引入 bug 的那一次提交。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/b8dc096c-4741-41b7-9839-397e47bac2c4.png" alt="图片"></p><p>我们知道二分法的效率是很高的，2的10次方就已经1024了，因此我们测试一般最多是10次，再多就是11次、12次。其实这就要求我们优化测试的方法，使得简单的操作就能使 bug 重现。如果重新的操作非常简单，简单到我们可以使用脚本就能测试，那就更轻松了，可以使用 git bisect run ./test.sh，一步到位。</p><p>如果某一个提交代码跑不起来，可以使用 git bisect skip 跳过当前提交或者使用 visualize 在 git 给出的列表中手动指定一个提交进行测试。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;1-开始&quot;&gt;&lt;a href=&quot;#1-开始&quot; class=&quot;headerlink&quot; title=&quot;1. 开始&quot;&gt;&lt;/a&gt;1. 开始&lt;/h1&gt;&lt;p&gt;大家下午好，欢迎大家来到 Coding 技术小馆，我叫谭贺贺，目前我在Coding 主要负责 WebIDE 与 Code
      
    
    </summary>
    
      <category term="git" scheme="http://yoursite.com/categories/git/"/>
    
    
      <category term="git" scheme="http://yoursite.com/tags/git/"/>
    
  </entry>
  
  <entry>
    <title>Git 客户端在 WebIDE 中的实现</title>
    <link href="http://yoursite.com/2016/05/03/how-implement-git-client-in-web-ide/"/>
    <id>http://yoursite.com/2016/05/03/how-implement-git-client-in-web-ide/</id>
    <published>2016-05-03T02:12:00.000Z</published>
    <updated>2017-09-27T02:10:06.387Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Git-客户端在-WebIDE-中的实现"><a href="#Git-客户端在-WebIDE-中的实现" class="headerlink" title="Git 客户端在 WebIDE 中的实现"></a>Git 客户端在 WebIDE 中的实现</h1><p>上周日做了一个分享，介绍了如何在 WebIDE 中实现 Git 客户端，主要内容包括 Git 原理、JGit 的使用。以下是主要的分享内容。</p><h2 id="版本控制"><a href="#版本控制" class="headerlink" title="版本控制"></a>版本控制</h2><p>管理文档、程序、配置等文件内容变化的的系统。</p><p>其实版本控制很想 并不难理解，其实即使不是编程人员对他也不会陌生，比如 windows 的系统还原，mac 的 timemachine。他们在某一时刻，记录下系统的状态或文件的内容，然后在需要的时候可以恢复。</p><p>对于程序员来说，他有以下好处：</p><ol><li><p>恢复：当不小心删除了文件、或者改错了文件，可以恢复文件内容</p></li><li><p>回滚：新版本出现了重大问题，可以回滚到上一正确的状态。</p></li><li><p>协作：不同开发者根据同一个版本进行开发，形成不同版本可以方便的合并在一起。</p></li></ol><h2 id="常见的版本控制系统"><a href="#常见的版本控制系统" class="headerlink" title="常见的版本控制系统"></a>常见的版本控制系统</h2><p>常见的版本控制系统有 CVS、SVN、Mercurial、Git 等。</p><p>这四个版本控制系统可以根据对网络的要求分成两组，一组是CVS、SVN，一组是 Mercurial、Git。</p><ul><li><p>CVS、SVN：使用中央仓库，开发者需要从中央仓库中取出代码</p></li><li><p>Mercurial、Git：使用本地仓库，开发者可以本地开发</p></li></ul><p>第一组要求必须连到公司的网络才能办公，而第二组仓库在本地，意味着不用连接到公司的网络，进一步可以说是离线就可以办公。</p><p>像Git、Mercurial这样的分布式的版本控制系统变得越来越流行，正在慢慢取代像CVS、SVN“中央式“的版本控制系统。</p><h2 id="为什么选择-Git"><a href="#为什么选择-Git" class="headerlink" title="为什么选择 Git"></a>为什么选择 Git</h2><p>是什么原因让 git 从这么多的版本控制系统中脱颖而出呢？</p><ol><li><p><strong>本地提交：</strong> 这意味着无论你是在家里、还是地铁上都可以离线工作了，不需要连到公司的网络。</p></li><li><p><strong>轻量级分支：</strong> git 的轻量级分支使得你可以快速的切换项目版本。这种特性在某些场景下特别重要，尤其是当我们正在开发过程中，突然发现一个紧急bug需要修复，我们可以快速切换分支，修复bug。</p></li><li><p><strong>解决冲突方便：</strong> 正因为有轻量级的分支，git也鼓励我们使用分支进行开发。但是当我们将分支合并到主干时，不可避免的会出现冲突，而 git 解决冲突的方式对用户非常的友好</p></li><li><p><strong>有 Github、Coding 这样强大的代码托管平台支持：</strong> 在 Github 和 Coding 上有非常多的开源代码，而且这两个平台上的用户非常的活跃，使用 git，有助于接触更多优秀的项目、优秀的开发者，对我们的成长有非常大的帮助。</p></li></ol><h1 id="Git-原理"><a href="#Git-原理" class="headerlink" title="Git 原理"></a>Git 原理</h1><h2 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h2><p>一段经典的 git 操作。</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">touch README.md</span><br><span class="line">git add README.md</span><br><span class="line">git commit -m <span class="string">"add readme"</span></span><br></pre></td></tr></table></figure><p><code>touch READEME.md</code> 可以代表创建、修改文件操作<br><code>git add README.md</code> 表示将对文件的改动添加到暂存区<br><code>git commit -m &quot;add readme&quot;</code>表示将改动提交到仓库  </p><p>这些我们都已经知道了，那么添加到暂存区、提交到仓库具体是什么意思？</p><h2 id="三种状态"><a href="#三种状态" class="headerlink" title="三种状态"></a>三种状态</h2><p> <img src="https://dn-coding-net-production-pp.qbox.me/9dcc8fa1-7217-425b-9102-46ad3b9275e3.png" alt="图片"></p><p>git 有三种状态：工作区、暂存区、本地仓库。</p><ul><li><p><strong>add:</strong> 工作区 -&gt; 暂存区</p></li><li><p><strong>commit:</strong> 暂存区 -&gt; 本地仓库</p></li></ul><p>工作目录我们是知道的，我们平时编写代码，就是在工作目录中完成的。</p><p>暂存区也叫做索引，保存了下次将提交的文件列表。</p><p>本地仓库是 Git 用来保存项目的数据的地方。提交代码，意味着将文件内容永久保存到数据库中。</p><p>首先看一下本地仓库，项目中的文件在本地仓库中是以快照的形式来保存的。</p><h2 id="git-中的快照"><a href="#git-中的快照" class="headerlink" title="git 中的快照"></a>git 中的快照</h2><p><img src="https://dn-coding-net-production-pp.qbox.me/f0aeb94f-6d70-40bc-b40b-7bbb1d7de688.png" alt="图片"></p><p>每一个 version，都是项目的一次完整快照。而快照中没有修改的文件，Git 使用链接指向之前存储的文件。</p><p>这就带来了一个问题，链接是什么？怎么快速的知道文件内容是否发生了改变？git 中的方案是使用 SHA-1。</p><h2 id="SHA-1"><a href="#SHA-1" class="headerlink" title="SHA-1"></a>SHA-1</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">'test content'</span> | git <span class="built_in">hash</span>-object --stdin</span><br><span class="line">d670460b4b4aece5915caf5c68d12f560a9fe3e4</span><br></pre></td></tr></table></figure><p>特点：</p><ul><li>由文件内容计算出的hash值</li><li>hash值相同，文件内容相同</li><li>作为唯一 id</li></ul><p>SHA-1将文件中的内容通过算法生成一个 160bit 的报文摘要，即40个十六进制数字。SHA-1的一个重要特征就是几乎可以保证，如果两个文件的SHA-1值是相同的，那么它们确是完全相同的内容。</p><p>上面的代码，无论运行几次，得到的 hash 值都是一样的。这个hash值可以看作是该文件的唯一id。</p><p>Git 中所有数据在存储前都计算该hash值，然后用该hash值来引用。因此这个 id 除了可以唯一表示任何版本中的文件，还可以表示任何一次提交、任何一次代码的快照。</p><h2 id="实际存储在-git-中的数据"><a href="#实际存储在-git-中的数据" class="headerlink" title="实际存储在 git 中的数据"></a>实际存储在 git 中的数据</h2><p><code>find .git/objects -type f</code></p><p><img src="https://dn-coding-net-production-pp.qbox.me/e494e331-4bde-4bc0-850f-678121f829c9.png" alt="图片"></p><p>我们来看一下实际存储在git中的数据，看起来比较乱，这些数据存放在 .git/objects，然后使用sha-1计算的hash值的前两位作为文件夹的名字，后面的38位作为文件的名字。</p><p>在这么多的文件中，其实可以分为4种类型，分别是 blob、commit、tree 和 tag。</p><p>将上面的内容经过按照这些类型整理可以得到类似下面的关系（忽略 tag）。</p><p><img src="https://dn-coding-net-production-pp.qbox.me/5eae25c3-7055-4d10-94ef-21c4ed1abc41.bmp" alt="图片"></p><p>每一个线框表示了一个object，也就是 objects 目录下的一个文件。</p><p>每个 object 上面的这个字母与数字组合的字符串，就是object的上一目录名＋文件名，也就是 sha-1 hash 值。</p><p>每个 object 的第一行格式是一致的，都由两列组成，第一列表示了 object 的类型，第二列是文件内容的长度。</p><p>接下来我们分别看一下每种类型：</p><p><strong>blob:</strong> 用来存放项目文件的内容，项目的任意文件的任意版本都是以 blob 的形式存放的。但是不包括文件的路径、名字、格式等其它描述信息。</p><p><strong>tree:</strong> 用来表示项目中的目录，我们知道，目录中有文件、有子目录。因此 tree 中有  blob、子 tree。这是与目录的对应。tree 中还包含了文件的路径以及名称。从顶层的 tree 纵览整个树状的结构，叶子结点就是blob，表示文件的内容，非叶子结点表示项目的目录，那么<strong>顶层的 tree 对象就代表了当前项目的快照</strong>。</p><p><strong>commit:</strong> 一个commit表示一次提交。里面的 tree 的值指向了项目的快照。还有一些其它的信息，比如 parent，committer、author、message 等信息。<br>tree 看成一个树状的结构，blob 可以作为其中的叶子结点出现。commit 可以看作是一个DAG，有向无环图。因为 commit 可以有一个 parent，也可以有两个或者多个parent。</p><p>至此，本地仓库我们就了解完了。接下来看一下暂存区。</p><h2 id="暂存区"><a href="#暂存区" class="headerlink" title="暂存区"></a>暂存区</h2><p>暂存区是工作区与本地仓库之间的一个缓冲，它保存了下次将提交的文件列表信息。它其实是一个文件，路径为： <code>.git/index</code>。由于该文件是一个二进制文件，没办法直接看它的内容，但是可以使用 git 命令查看。</p><p><img src="https://dn-coding-net-production-pp.qbox.me/a2897509-382d-4a2a-9d94-4c4d8e8a4051.bmp" alt="图片"></p><p>每列的含义依次为，文件权限、文件 blob、文件状态、文件名。</p><p>第二列指的是文件的 blob。这个 blob 存放了文件暂存时的内容。</p><p>我们操作暂存区的场景是这样的，每当编辑好一个或几个文件后，把它加入到暂存区，然后接着修改其他文件，改好后放入暂存区，循环反复。直到修改完毕，最后使用 commit 命令，将暂存区的内容永久保存到本地仓库。</p><p>这个过程其实就是构建项目快照的过程，因此可以说暂存区是用来构建项目快照的区域。</p><h2 id="分支"><a href="#分支" class="headerlink" title="分支"></a>分支</h2><p>接下来看一下分支的概念，首先看一张图：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/128e6044-9bbb-4b84-9ac7-5552c6a946ec.png" alt="图片"></p><p>这张图中的每一个点表示了一个commit。从这张图中我们可以看出的信息有：</p><ul><li><p>从任意一点分歧出来的线都可以叫做分支</p></li><li><p>分支可以合并</p></li></ul><h2 id="分支的实现"><a href="#分支的实现" class="headerlink" title="分支的实现"></a>分支的实现</h2><p>在 .git/HEAD 文件中，保存了当前的分支。</p><figure class="highlight excel"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cat .git/HEAD</span><br><span class="line">=&gt;r<span class="symbol">ef:</span> refs/heads/master</span><br></pre></td></tr></table></figure><p>其实这个 ref 表示的就是一个分支，它也是一个文件，我们可以继续看一下这个文件的内容：</p><figure class="highlight excel"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">cat .git/refs/heads/master</span><br><span class="line">=&gt; <span class="number">2</span>b388d2c1c20998b6233ff47596b0c87ed3ed8f8</span><br></pre></td></tr></table></figure><p>可以看到分支存储了一个 object，我们可以使用 cat-file 命令继续查看该 object 的内容。</p><figure class="highlight excel"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">git cat-file -p <span class="number">2</span>b388d2c1c20998b6233ff47596b0c87ed3ed8f8</span><br><span class="line">=&gt; tree <span class="number">15</span>f880be0567a8844291459f90e9d0004743c8d9</span><br><span class="line">=&gt; parent <span class="number">3</span>d885a272478d0080f6d22018480b2e83ec2c591</span><br><span class="line">=&gt; author Hehe <span class="built_in">Tan</span> &lt;xiayule148@gmail.com&gt; <span class="number">1460971725</span> +<span class="number">0800</span></span><br><span class="line">=&gt; committer Hehe <span class="built_in">Tan</span> &lt;xiayule148@gmail.com&gt; <span class="number">1460971725</span> +<span class="number">0800</span></span><br><span class="line">=&gt;</span><br><span class="line">=&gt; add branch paramter for rebase</span><br></pre></td></tr></table></figure><p>从上面的内容，我们知道了分支指向了一次提交。为什么分支指向一个提交的原因，其实也是git中的分支为什么这么轻量的答案。</p><p>因为分支就是指向了一个 commit 的指针，当我们提交新的commit，这个分支的指向只需要跟着更新就可以了，而创建分支仅仅是创建一个指针。</p><p>至此git的原理就讲完了，接下来看一下 JGit。</p><h1 id="JGit"><a href="#JGit" class="headerlink" title="JGit"></a>JGit</h1><p>JGit 是一个用 Java 实现的比较健全的 git 实现，Eclipse IDE 中的 git 插件 Egit，就是基于 JGit 开发的。同 git 一样，它提供了底层命令和高层命令。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/1935c742-57af-4205-9fa1-d42b54323c54.png" alt="图片"></p><p>高层命令的入口是 Git 类。高层命令好理解，我们使用 git 的客户端绝大多数命令都是高层命令。</p><p>比如 add、commit、checkout 等都是高层命令，他们提供了友好的交互，往往一条命令就能完成你所想要的效果。</p><p>底层命令的入口是 Repository 类。底层命令不同于高层命令，它们直接作用域 仓库（Repository）。比如 AbstractTreeIterator，就是用来遍历 Tree 结构的，DirCache 是用来操作暂存区的，RevWalk 是用来遍历 commit 的，ObjectInsert 是用来生成 obj的，ObjectLoader 是用来加载 object。</p><p>一条高层命令往往是由多条底层命令组成的。</p><h2 id="Repository-仓库"><a href="#Repository-仓库" class="headerlink" title="Repository(仓库)"></a>Repository(仓库)</h2><p>作为一切的开始，你需要一个 Repository。</p><figure class="highlight haxe"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Repository repository = <span class="keyword">new</span> <span class="type">FileRepositoryBuilder</span>()</span><br><span class="line">                .setGitDir(<span class="keyword">new</span> <span class="type">File</span>(<span class="string">"/home/tan/GitTest/.git"</span>))</span><br><span class="line">                .readEnvironment()</span><br><span class="line">                .build();</span><br></pre></td></tr></table></figure><p>使用时只需要将仓库的路径传进来就可以了，它会自动读取一些必要的环境变量。</p><h2 id="ObjectInserter"><a href="#ObjectInserter" class="headerlink" title="ObjectInserter"></a>ObjectInserter</h2><p>ObjectInserter用来将数据插入到git数据库中，也就是 objects 目录下。插入的类型为我们刚才提到的四种，分别是 Blob、Tree、Commit、Tag。</p><figure class="highlight haxe"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> (ObjectInserter inserter = repo.<span class="keyword">new</span><span class="type">ObjectInserter</span>()) &#123;</span><br><span class="line">    ObjectId objectId = inserter.insert(Constants.OBJ_BLOB,</span><br><span class="line">        <span class="keyword">new</span> <span class="type">String</span>(<span class="string">"test"</span>).getBytes());</span><br><span class="line">    inserter.flush();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>第二个参数表示要插入的数据，该数据会自动使用 zlib 压缩。</p><h2 id="TreeWalk"><a href="#TreeWalk" class="headerlink" title="TreeWalk"></a>TreeWalk</h2><p>用来遍历目录结构，可以为工作区、暂存区或项目快照（版本库）。</p><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">try</span> (TreeWalk treeWalk = <span class="keyword">new</span> TreeWalk(repo)) &#123;</span><br><span class="line">    treeWalk.setRecursive(<span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line">    treeWalk.addTree(<span class="keyword">new</span> FileTreeIterator(repo));</span><br><span class="line">    treeWalk.addTree(<span class="keyword">new</span> DirCacheIterator(repo.readDirCache()));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (treeWalk.<span class="keyword">next</span>()) &#123;</span><br><span class="line">        AbstractTreeIterator treeIterator</span><br><span class="line">                = treeWalk.getTree(<span class="number">0</span>, AbstractTreeIterator.<span class="keyword">class</span>);</span><br><span class="line">        DirCacheIterator dirCacheIterator</span><br><span class="line">                = treeWalk.getTree(<span class="number">1</span>, DirCacheIterator.<span class="keyword">class</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>TreeWalk 是用来遍历树这种结构的，它比较厉害的一点是可以同时遍历多棵树，遍历多课树的思路为将文件列表做一个合并，然后遍历这个列表，没有的调用 getTree 会返回 null 值。</p><p>其实 git status 就是这种原理来做的:</p><ol><li>changed： 在版本库、idnex 中都存在，内容不同</li><li>removed： 在版本库存在，在 index 不存在</li><li>added：在 index 存在，在版本库不存在</li><li>untracked：不在版本库和 index，只在工作目录中存在</li><li>modified：在 index，在工作区，且文件内容不同</li><li>missing：在 index 存在，在工作区不存在</li></ol><h2 id="RevWalk"><a href="#RevWalk" class="headerlink" title="RevWalk"></a>RevWalk</h2><p>RevWalk 用来遍历 Commit。</p><figure class="highlight armasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="symbol">try</span> (<span class="keyword">RevWalk </span><span class="keyword">revWalk </span>= new <span class="keyword">RevWalk(repository)) </span>&#123;</span><br><span class="line">    <span class="keyword">revWalk.markStart(one);</span></span><br><span class="line"><span class="keyword"> </span>   <span class="keyword">revWalk.markStart(two);</span></span><br><span class="line"><span class="keyword"></span></span><br><span class="line"><span class="keyword"> </span>   <span class="keyword">revWalk.setRevFilter(RevFilter.MERGE_BASE);</span></span><br><span class="line"><span class="keyword"></span></span><br><span class="line"><span class="keyword"> </span>   <span class="keyword">RevCommit </span><span class="keyword">base </span>= <span class="keyword">revWalk.next();</span></span><br><span class="line"><span class="keyword">&#125;</span></span><br></pre></td></tr></table></figure><p>我们这个例子，标记了两个 commit，我们设置的 filter 是 MERGE_BASE, 它会自动查找这两个 commit 所在分支的 MERGE_BASE。其中 MERGE_BASE 可以看作是分支的分岔点，合并的时候 MERGE_BASE 会作为参照。</p><h2 id="使用底层命令"><a href="#使用底层命令" class="headerlink" title="使用底层命令"></a>使用底层命令</h2><p>高层命令其实是由多条底层命令组成的，比如我们最常使用的 add、commit：</p><ul><li><p>add</p><ul><li>使用 ObjectInserter 将文件内容写入 objects (blob)，得到 blob id</li><li>使用 DirCache 将 blob id 写入暂存区</li></ul></li><li><p>commit</p><ul><li>使用 DirCache 将 index 生成 tree</li><li>使用 ObjectInserter 将 tree 写入仓库(tree)，得到 tree 的 id</li><li>构建 commit，写入 tree id 以及设置其 parent、message 等其它信息</li><li>利用 ObjectInserter 将 commit 写入 objects (commit)，得到 commit id</li><li>将 commit id 写入当前的 branch，使得 branch 指向最新的 commit</li></ul></li></ul><h2 id="高层命令"><a href="#高层命令" class="headerlink" title="高层命令"></a>高层命令</h2><p>上面的复杂操作，可以简单的用底层命令替代。</p><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">git</span><span class="selector-class">.add</span>()<span class="selector-class">.addFilepattern</span>(<span class="string">"README"</span>)<span class="selector-class">.call</span>();</span><br><span class="line"><span class="selector-tag">git</span><span class="selector-class">.commit</span>()<span class="selector-class">.setMessage</span>(<span class="string">"add readme"</span>)<span class="selector-class">.call</span>();</span><br></pre></td></tr></table></figure><h1 id="高级操作的局限"><a href="#高级操作的局限" class="headerlink" title="高级操作的局限"></a>高级操作的局限</h1><p>高层命令使用起来方便，但是它所提供的功能有限。这里我们拿 merge 举例。</p><h2 id="使用-JGit-Merge-api"><a href="#使用-JGit-Merge-api" class="headerlink" title="使用 JGit Merge api"></a>使用 JGit Merge api</h2><p>使用 JGit 提供的接口进行 merge 十分的方便，只需要指定要合并的 branch 就可以了。</p><figure class="highlight n1ql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">MergeCommand <span class="keyword">merge</span> = git.<span class="keyword">merge</span>();</span><br><span class="line"><span class="keyword">merge</span>.<span class="keyword">include</span>(branch);</span><br><span class="line">MergeResult result = <span class="keyword">merge</span>.<span class="keyword">call</span>();</span><br></pre></td></tr></table></figure><p>但是 merge 之后呢，文件冲突了怎么办，怎么解决冲突呢？实际上除了 merge，stash、rebase 等等操作也都会产生冲突。也就是说 git 冲突文件的处理是客户端的重要功能之一。</p><p>遗憾的是 jgit 并没有提供解决冲突的方案，所以这就需要我们自己来解决这个问题。</p><h2 id="resolve-conflicts"><a href="#resolve-conflicts" class="headerlink" title="resolve conflicts:"></a>resolve conflicts:</h2><p>一种比较理想的解决冲突的方案是，将冲突的文件根据本地修改、基础版本、要合并分支的修改分成三栏。</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/713886dd-e199-453c-9863-c65457fddb14.png" alt="图片"></p><p>通过这样的方式，我们可以直观的对照冲突的内容，并且可以方便的选取或者要抛弃修改。</p><h2 id="可选方案"><a href="#可选方案" class="headerlink" title="可选方案"></a>可选方案</h2><ol><li><p>计算 merge base</p><p> 第一个就是计算这两个分支的 MERGE_BASE。这样我们获得了三个 commit，每个 commit 都都纪录了提交时的文件快照。而我们只要将冲突文件的内容从快照中取出来就好了。但是这个方案有个缺点，那就是我们只有在合并的那一瞬间才能知道要合并的分支，之后想要知道只能去 .git 下面的 MERGE_HEAD 去查，而且其它方式比如 stash、rebase 等操作引起的冲突是不会生成该文件的。</p></li><li><p>使用暂存区的信息</p><p> 想想我们 当我们有合并冲突状态时，使用 git status，会列出冲突文件，以及冲突的类型，比如 “双方修改”、“由我们删除”，“双方添加”等这样的字眼，git 如果获得这些信息的呢？</p><p> 如果存在冲突文件，我们查看暂存区，可以看到类似下面的内容：</p> <figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">git ls-files --stage</span><br><span class="line"><span class="number">100644</span> <span class="number">6e9</span>f0da13f19b444ec3a9c3d6e795ad35c0554a2 <span class="number">1</span>Readme</span><br><span class="line"><span class="number">100644</span> <span class="number">29</span>d460866c44ad72cc08ef4983fc6ebd48053bab <span class="number">2</span>Readme</span><br><span class="line"><span class="number">100644</span> <span class="number">12892</span>f544e81ef2170034392f63c7fc5e6c6ccd9 <span class="number">3</span>Readme</span><br></pre></td></tr></table></figure><p> 原来暂存区中有四种状态用于标示文件：    </p><pre><code>* **0:** standard stage* **1:** base tree revision* **2:** first tree revision (usually called &quot;ours&quot;)* **3:** second tree revision (usually called &quot;theirs&quot;)</code></pre><p> 接下来我们专门看一下这4种状态是如何表示冲突状态的。</p></li></ol><h2 id="文件冲突的状态："><a href="#文件冲突的状态：" class="headerlink" title="文件冲突的状态："></a>文件冲突的状态：</h2><p>假设当前我们处于 master 分支，要合并的分支为 test，开发历史如下图：</p><p> <img src="https://dn-coding-net-production-pp.qbox.me/c3374133-939a-4f35-9ff3-df1cfa87c284.png" alt="图片"></p><p>现在假设合并过程中有个文件（Readme）发生了冲突，我们查询暂存区该文件的状态（可以有多个）：</p><ul><li><strong>1 and 2:</strong> DELETED_BY_THEM;</li><li><strong>1 and 3:</strong> DELETED_BY_US;</li><li><strong>2 and 3:</strong> BOTH_ADDED;</li><li><strong>1 and 2 and 3</strong>: BOTH_MODIFIED</li></ul><p>我们拿第一种情况举例，文件（Readme）有两种状态 1 和 2，1 表示该文件存在于 commit 1（也就是MERGE_BASE），2 表示该文件在 commit 2 （master 分支）中被修改了，没有状态 3，也就是该文件在 commit 3（test分支）被删除了，总结来说这种状态就是 DELETED_BY_THEM。</p><p>可以再看一下第四种情况，文件（Readme）有三种状态 1、2、3，1 表示 commit 1（MERGE_BASE）中存在，2 表示 commit 2（master 分支）进行了修改，3 表示（test 分支）也就行了修改，总结来说就是 BOTH_MODIFIED（双方修改）。</p><h2 id="获取冲突文件的三个版本"><a href="#获取冲突文件的三个版本" class="headerlink" title="获取冲突文件的三个版本"></a>获取冲突文件的三个版本</h2><p>知道了冲突文件的状态，就能在暂存区获得冲突文件的三个版本了。代码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line">DirCache dirCache = repository.readDirCache();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在暂存区中，所有文件是按照字母顺序排列的，因此文件的不同状态是连着的</span></span><br><span class="line"><span class="keyword">int</span> eIdx = dirCache.findEntry(path);</span><br><span class="line"><span class="comment">// nextEntry 会自动调过文件名相同的文件，找到下一个文件。</span></span><br><span class="line"><span class="keyword">int</span> lastIdx = dirCache.nextEntry(eIdx);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在 [eIdx, lastIdx) 区间的也就是文件的冲突的不同版本</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;lastIdx - eIdx; i++) &#123;</span><br><span class="line">    DirCacheEntry entry = dirCache.getEntry(eIdx + i);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 如果是 MERGE_BASE</span></span><br><span class="line">    <span class="keyword">if</span> (entry.getStage() == DirCacheEntry.STAGE_1)</span><br><span class="line">        readBlobContent(entry.getObjectId());</span><br><span class="line">    <span class="comment">// 如果是 当前分支</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (entry.getStage() == DirCacheEntry.STAGE_2)</span><br><span class="line">        readBlobContent(entry.getObjectId());</span><br><span class="line">    <span class="comment">// 如果是 要合并的分支</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (entry.getStage() == DirCacheEntry.STAGE_3)</span><br><span class="line">        readBlobContent(entry.getObjectId());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>至此我们得到了解决合并冲突的一个方案。</p><h1 id="Thanks"><a href="#Thanks" class="headerlink" title="Thanks"></a>Thanks</h1>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;Git-客户端在-WebIDE-中的实现&quot;&gt;&lt;a href=&quot;#Git-客户端在-WebIDE-中的实现&quot; class=&quot;headerlink&quot; title=&quot;Git 客户端在 WebIDE 中的实现&quot;&gt;&lt;/a&gt;Git 客户端在 WebIDE 中的实现&lt;/h1&gt;&lt;
      
    
    </summary>
    
      <category term="git" scheme="http://yoursite.com/categories/git/"/>
    
    
      <category term="git" scheme="http://yoursite.com/tags/git/"/>
    
      <category term="java" scheme="http://yoursite.com/tags/java/"/>
    
  </entry>
  
  <entry>
    <title>Java 项目优化实战</title>
    <link href="http://yoursite.com/2016/03/27/how-to-optimize/"/>
    <id>http://yoursite.com/2016/03/27/how-to-optimize/</id>
    <published>2016-03-27T13:55:00.000Z</published>
    <updated>2017-09-27T02:11:06.977Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-Visual-VM"><a href="#1-Visual-VM" class="headerlink" title="1 Visual VM"></a>1 Visual VM</h2><p>项目中的某一个接口，在某一场景下（数据量大），性能让人难以忍受。</p><p>那么如何有什么工具可以定位引发性能问题的代码呢？其实有很多，这里我们使用 Visual VM。</p><img src="/2016/03/27/how-to-optimize/bg2016031203.png"><p>Visual VM 是一款用来分析 Java 应用的图形工具，能够对 Java 应用程序做性能分析和调优。如果你使用的 java 7 或者 java 8，那么可以直接在 JDK 的 bin 目录找到该工具，名称为 jvisualvm。当然也可以在<a href="http://visualvm.java.net/" target="_blank" rel="external">官网</a>上自行下载。</p><p>使用 Visual VM 分析某个接口的性能的方法如下：</p><img src="/2016/03/27/how-to-optimize/bg2016031204.png"><p>结果显示如下:</p><img src="/2016/03/27/how-to-optimize/bg2016031201.png"><p>通过上图，我们可以看到比较耗时的方法为 resolveBytePosition 和 rest，getFile 和 currentUser 是网络请求，暂不考虑。</p><h2 id="2-优化一"><a href="#2-优化一" class="headerlink" title="2 优化一"></a>2 优化一</h2><h3 id="2-1-背景"><a href="#2-1-背景" class="headerlink" title="2.1 背景"></a>2.1 背景</h3><p>首先拿 resolveBytePosition 方法开刀。为了能更容易的解释 resolveBytePosition 的用途，举个例子。</p><p>给定一个字符串 chars 与该字符串的 UTF-8 二进制数组(空格用来隔开字符数据，实际并不存在):</p><pre><code>chars = &quot;just一个test&quot;;bytes = &quot;6A 75 73 74 E4B880 E4B8AA 74 65 73 74&quot;;</code></pre><p>resolveBytePosition 用来解决给定一个 bytes 的偏移 bytePos 计算 chars 中的偏移 charPos 的问题。比如:</p><pre><code>bytePos = 0 (6A) 对应 charPos = 0 (j)bytePos = 1 (75) 对应 charPos = 1 (u)</code></pre><p>如果使用 array[start:] 表示从下标 start 开始截取数组元素至末尾组成的新数组，那么则有：</p><pre><code>bytes[bytePos:] = chars[charPos:]</code></pre><p>举例:</p><pre><code>bytes[0:] = chars[0:]bytes[1:] = chars[1:]bytes[10:] = chars[6:]</code></pre><h3 id="2-2-原实现"><a href="#2-2-原实现" class="headerlink" title="2.2 原实现"></a>2.2 原实现</h3><p>明白了 resolveBytePosition 的作用，看一下它的实现</p><figure class="highlight arduino"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">int</span> resolveBytePosition(<span class="keyword">byte</span>[] bytes, <span class="keyword">int</span> bytePos) &#123;</span><br><span class="line">    <span class="built_in">return</span> <span class="keyword">new</span> <span class="keyword">String</span>(slice(bytes, <span class="number">0</span>, bytePos)).length();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>该解法简单粗暴，能够准确的计算出结果，但是缺点显而易见，频繁的构建字符串，对性能造成了极大的影响。通过 Visual VM 可以证实我们的推论，通过点击快照，查看更详细的方法调用耗时。</p><img src="/2016/03/27/how-to-optimize/bg2016031205.png"><h3 id="2-3-剖析"><a href="#2-3-剖析" class="headerlink" title="2.3 剖析"></a>2.3 剖析</h3><p>为了更方便的剖析问题，我们绘制如下表格，用来展示每一个字符的 UTF-8 以及 Unicode 的二进制数据：</p><table><thead><tr><th style="text-align:center"></th><th style="text-align:center">j</th><th style="text-align:center">u</th><th style="text-align:center">s</th><th style="text-align:center">t</th><th style="text-align:center">一</th><th style="text-align:center">个</th><th style="text-align:center">t</th><th style="text-align:center">e</th><th style="text-align:center">s</th><th style="text-align:center">t</th></tr></thead><tbody><tr><td style="text-align:center">UTF-8</td><td style="text-align:center">6A</td><td style="text-align:center">75</td><td style="text-align:center">73</td><td style="text-align:center">74</td><td style="text-align:center">E4B880</td><td style="text-align:center">E4B8AA</td><td style="text-align:center">74</td><td style="text-align:center">65</td><td style="text-align:center">73</td><td style="text-align:center">74</td></tr><tr><td style="text-align:center">Unicode</td><td style="text-align:center">6A</td><td style="text-align:center">75</td><td style="text-align:center">73</td><td style="text-align:center">74</td><td style="text-align:center">4E00</td><td style="text-align:center">4E2A</td><td style="text-align:center">74</td><td style="text-align:center">65</td><td style="text-align:center">73</td><td style="text-align:center">74</td></tr></tbody></table><p>接着我们将字节数据转换为字节长度：</p><table><thead><tr><th style="text-align:center"></th><th style="text-align:center">j</th><th style="text-align:center">u</th><th style="text-align:center">s</th><th style="text-align:center">t</th><th style="text-align:center">一</th><th style="text-align:center">个</th><th style="text-align:center">t</th><th style="text-align:center">e</th><th style="text-align:center">s</th><th style="text-align:center">t</th></tr></thead><tbody><tr><td style="text-align:center">UTF-8</td><td style="text-align:center">1</td><td style="text-align:center">1</td><td style="text-align:center">1</td><td style="text-align:center">1</td><td style="text-align:center">3</td><td style="text-align:center">3</td><td style="text-align:center">1</td><td style="text-align:center">1</td><td style="text-align:center">1</td><td style="text-align:center">1</td></tr><tr><td style="text-align:center">Unicode</td><td style="text-align:center">1</td><td style="text-align:center">1</td><td style="text-align:center">1</td><td style="text-align:center">1</td><td style="text-align:center">2</td><td style="text-align:center">2</td><td style="text-align:center">1</td><td style="text-align:center">1</td><td style="text-align:center">1</td><td style="text-align:center">1</td></tr></tbody></table><p>Java中的使用 char 来表示Unicode，char 的长度为 2 个字节，因此一个 char 足以表示示例中的任何一个字符。</p><p>我们使用一个单元格表示一个byte（UTF-8）或一个char（Unicode），并对单元格编号，得到下表：</p><table><thead><tr><th style="text-align: center">&nbsp;</th><th style="text-align: center">j</th><th style="text-align: center">u</th><th style="text-align: center">s</th><th style="text-align: center">t</th><th style="text-align: center" colspan="3">一</th><th style="text-align: center" colspan="3">个</th><th style="text-align: center">t</th><th style="text-align: center">e</th><th style="text-align: center">s</th><th style="text-align: center">t</th></tr></thead><tbody><tr></tr><tr><td style="text-align: center">bytes</td><td style="text-align: center">0</td><td style="text-align: center">1</td><td style="text-align: center">2</td><td style="text-align: center">3</td><td style="text-align: center">4</td><td style="text-align: center">5</td><td style="text-align: center">6</td><td style="text-align: center">7</td><td style="text-align: center">8</td><td style="text-align: center">9</td><td style="text-align: center">10</td><td style="text-align: center">11</td><td style="text-align: center">12</td><td style="text-align: center">13</td></tr><td style="text-align: center">chars</td><td style="text-align: center">0</td><td style="text-align: center">1</td><td style="text-align: center">2</td><td style="text-align: center">3</td><td style="text-align: center" colspan="3">4</td><td style="text-align: center" colspan="3">5</td><td style="text-align: center">6</td><td style="text-align: center">7</td><td style="text-align: center">8</td><td style="text-align: center">9</td></tbody></table><p>可以得出下面对应关系</p><pre><code>bytes[0:] = chars[0:]bytes[1:] = chars[1:]bytes[2:] = chars[2:]bytes[3:] = chars[3:]bytes[4:] = chars[4:]bytes[7:] = chars[5:]bytes[10:] = chars[6:]... ...</code></pre><h3 id="2-3-方案"><a href="#2-3-方案" class="headerlink" title="2.3 方案"></a>2.3 方案</h3><p>进行到这一步，高效的算法已经呼之欲出了。算法如下：</p><p>把字符 UTF-8 数据的二进制长度不为 1 的称为特征点。除特征点外，每个字符都是一个字节长度。记下所有特征点的对应关系，对于给定的 bytePos，都可以根据公式计算得到 charPos。</p><p>公式为:</p><pre><code>charPos = bytePos - preBytePos + preCharPos</code></pre><p>举例：</p><p>则本实例中有两个特征点 <code>一</code>、<code>个</code>，记作：</p><pre><code>bytes[6:] = chars[4:]bytes[9:] = chars[5:]</code></pre><p>如果给定 bytePos 10, 首先找到前一个特征点的对应关系 9（preBytePos） -&gt; 5（preCharPos), 根据公式得出 (10 - 9) + 5 = 6。</p><h3 id="2-4-核心代码"><a href="#2-4-核心代码" class="headerlink" title="2.4 核心代码"></a>2.4 核心代码</h3><p>该算法还有一个比较关键的问题要解决，即高效的计算一个 char 的字节长度。计算 char 的字节长度的算法参考了 <a href="http://stackoverflow.com/questions/2726071/efficient-way-to-calculate-byte-length-of-a-character-depending-on-the-encoding" target="_blank" rel="external">StackOverflow</a>。</p><figure class="highlight verilog"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 计算特征点</span></span><br><span class="line">private <span class="keyword">int</span>[][] calcSpecialPos(String str) &#123;</span><br><span class="line">    ArrayList&lt;<span class="keyword">int</span>[]&gt; specialPos = <span class="keyword">new</span> ArrayList&lt;&gt;()</span><br><span class="line"></span><br><span class="line">    specialPos<span class="variable">.add</span>(<span class="keyword">new</span> <span class="keyword">int</span>[] &#123;<span class="number">0</span>, <span class="number">0</span>&#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> lastCharPost = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> lastBytePos = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    Charset utf8 = Charset<span class="variable">.forName</span>(<span class="string">"UTF-8"</span>);</span><br><span class="line">    CharsetEncoder encoder = utf8<span class="variable">.newEncoder</span>();</span><br><span class="line">    CharBuffer <span class="keyword">input</span> = CharBuffer<span class="variable">.wrap</span>(str<span class="variable">.toCharArray</span>());</span><br><span class="line">    ByteBuffer <span class="keyword">output</span> = ByteBuffer<span class="variable">.allocate</span>(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> limit = <span class="keyword">input</span><span class="variable">.limit</span>();</span><br><span class="line">    <span class="keyword">while</span>(<span class="keyword">input</span><span class="variable">.position</span>() &lt; limit) &#123;</span><br><span class="line">        <span class="keyword">output</span><span class="variable">.clear</span>();</span><br><span class="line">        <span class="keyword">input</span><span class="variable">.mark</span>();</span><br><span class="line">        <span class="keyword">input</span><span class="variable">.limit</span>(Math<span class="variable">.min</span>(<span class="keyword">input</span><span class="variable">.position</span>() + <span class="number">2</span>, <span class="keyword">input</span><span class="variable">.capacity</span>()));</span><br><span class="line">        <span class="keyword">if</span> (Character<span class="variable">.isHighSurrogate</span>(<span class="keyword">input</span><span class="variable">.get</span>()) &amp;&amp; !Character<span class="variable">.isLowSurrogate</span>(<span class="keyword">input</span><span class="variable">.get</span>())) &#123;</span><br><span class="line">            <span class="comment">//Malformed surrogate pair</span></span><br><span class="line">            lastCharPost++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">input</span><span class="variable">.limit</span>(<span class="keyword">input</span><span class="variable">.position</span>());</span><br><span class="line">        <span class="keyword">input</span><span class="variable">.reset</span>();</span><br><span class="line">        encoder<span class="variable">.encode</span>(<span class="keyword">input</span>, <span class="keyword">output</span>, false);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> encodedLen = <span class="keyword">output</span><span class="variable">.position</span>();</span><br><span class="line">        lastCharPost++;</span><br><span class="line">        lastBytePos += encodedLen;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (encodedLen != <span class="number">1</span>) &#123; <span class="comment">// 特征点</span></span><br><span class="line">            specialPos<span class="variable">.add</span>(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;lastBytePos, lastCharPost&#125;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> toArray(specialPos);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 根据特征点，计算 bytePos 对应的 charPos</span></span><br><span class="line">private <span class="keyword">int</span> calcPos(<span class="keyword">int</span>[][] specialPos, <span class="keyword">int</span> bytePos) &#123;</span><br><span class="line">    <span class="comment">// 如果只有一个元素 &#123;0, 0)，说明没有特征值</span></span><br><span class="line">    <span class="keyword">if</span> (specialPos<span class="variable">.length</span> == <span class="number">1</span>) <span class="keyword">return</span> bytePos;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> pos = Arrays<span class="variable">.binarySearch</span>(specialPos,</span><br><span class="line">            <span class="keyword">new</span> <span class="keyword">int</span>[] &#123;bytePos, <span class="number">0</span>&#125;,</span><br><span class="line">            (<span class="keyword">int</span>[] a, <span class="keyword">int</span>[] b) -&gt; Integer<span class="variable">.compare</span>(a[<span class="number">0</span>], b[<span class="number">0</span>]));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (pos &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> specialPos[pos][<span class="number">1</span>];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// if binary search not fonund, will return (-(insertion point) - 1),</span></span><br><span class="line">        <span class="comment">// so here -2 is mean -1 to get insertpoint and then -1 to get previous specialPos</span></span><br><span class="line">        <span class="keyword">int</span>[] preSpecialPos = specialPos[-pos-<span class="number">2</span>];</span><br><span class="line">        <span class="keyword">return</span> bytePos - preSpecialPos[<span class="number">0</span>] + preSpecialPos[<span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="3-优化二"><a href="#3-优化二" class="headerlink" title="3 优化二"></a>3 优化二</h2><h3 id="3-1-背景"><a href="#3-1-背景" class="headerlink" title="3.1 背景"></a>3.1 背景</h3><p>接下来解决第二个函数 rest。该函数的功能是得到 JsonArray（gson） 的除第一个元素外的所有元素。</p><p>由于 rest 是在一个递归函数中被调用且递归栈很深，因此如果 rest 实现的不够高效，其影响会被成倍放大。</p><h3 id="3-2-原实现"><a href="#3-2-原实现" class="headerlink" title="3.2 原实现"></a>3.2 原实现</h3><figure class="highlight processing"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> JsonArray rest(JsonArray arr) &#123;</span><br><span class="line">    JsonArray result = <span class="keyword">new</span> JsonArray();</span><br><span class="line">    <span class="keyword">if</span> (arr.<span class="built_in">size</span>() &gt; <span class="number">1</span>) &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">1</span>; i &lt; arr.<span class="built_in">size</span>(); i++) &#123;</span><br><span class="line">            result.<span class="built_in">add</span>(arr.<span class="built_in">get</span>(i));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-3-剖析"><a href="#3-3-剖析" class="headerlink" title="3.3 剖析"></a>3.3 剖析</h3><p>通过调试发现 JsonArray 中存储了相当大的数据，对于频繁调用的场景，每次都对其重新构建明显不是一个明智的选择。<br>通过查看返回的 JsonArray 使用情况，我们得到了另一条线索：仅仅使用里面的数据，而不涉及修改。</p><p>考虑到 JsonArray 被实现成 final，最后方案确定为实现一个针对 rest 这种需求定制的代理类。</p><h3 id="3-4-方案-amp-代码"><a href="#3-4-方案-amp-代码" class="headerlink" title="3.4 方案 &amp; 代码"></a>3.4 方案 &amp; 代码</h3><p>代理类 JsonArrayWrapper 分别对 first、rest、foreach 等功能进行了实现。</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">JsonArrayWrapper</span> <span class="keyword">implements</span> <span class="title">Iterable</span>&lt;<span class="title">JsonElement</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> JsonArray jsonArray;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> mark;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">JsonArrayWrapper</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.jsonArray = <span class="keyword">new</span> JsonArray();</span><br><span class="line">        <span class="keyword">this</span>.mark = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">JsonArrayWrapper</span><span class="params">(JsonArray jsonArray)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.jsonArray= jsonArray;</span><br><span class="line">        <span class="keyword">this</span>.mark = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">JsonArrayWrapper</span><span class="params">(JsonArray jsonArray, <span class="keyword">int</span> mark)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.jsonArray = jsonArray;</span><br><span class="line">        <span class="keyword">this</span>.mark = mark;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function">JsonObject <span class="title">first</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> jsonArray.get(mark).getAsJsonObject();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function">JsonArrayWrapper <span class="title">rest</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> JsonArrayWrapper(jsonArray, mark+<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">int</span> <span class="title">size</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> jsonArray.size() - mark;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function">JsonElement <span class="title">get</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">return</span> jsonArray.<span class="title">get</span><span class="params">(mark + n)</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">add</span><span class="params">(JsonElement jsonElement)</span> </span>&#123;</span><br><span class="line">        jsonArray.add(jsonElement);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">addAll</span><span class="params">(JsonArrayWrapper jsonArrayWrapper)</span> </span>&#123;</span><br><span class="line">        jsonArrayWrapper.forEach(<span class="keyword">this</span>.jsonArray::add);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Iterator&lt;JsonElement&gt; iterator() &#123;</span><br><span class="line">        JsonArray jsonarray = <span class="keyword">new</span> JsonArray();</span><br><span class="line">        <span class="keyword">this</span>.forEach(e -&gt; jsonarray.add(e));</span><br><span class="line">        <span class="function"><span class="keyword">return</span> jsonarray.<span class="title">iterator</span><span class="params">()</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">void</span> <span class="title">forEach</span><span class="params">(Consumer&lt;? <span class="keyword">super</span> JsonElement&gt; action)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i=mark; i&lt;jsonArray.size(); i++) &#123;</span><br><span class="line">            action.accept(jsonArray.get(i));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="4-成果"><a href="#4-成果" class="headerlink" title="4 成果"></a>4 成果</h2><p>经过这两个主要的优化，就解决了代码中的性能问题，成果如下图所示：</p><img src="/2016/03/27/how-to-optimize/bg2016031202.png">]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;1-Visual-VM&quot;&gt;&lt;a href=&quot;#1-Visual-VM&quot; class=&quot;headerlink&quot; title=&quot;1 Visual VM&quot;&gt;&lt;/a&gt;1 Visual VM&lt;/h2&gt;&lt;p&gt;项目中的某一个接口，在某一场景下（数据量大），性能让人难以忍受。&lt;/
      
    
    </summary>
    
      <category term="java" scheme="http://yoursite.com/categories/java/"/>
    
    
      <category term="java" scheme="http://yoursite.com/tags/java/"/>
    
      <category term="optimize" scheme="http://yoursite.com/tags/optimize/"/>
    
  </entry>
  
  <entry>
    <title>Java 与 Unicode</title>
    <link href="http://yoursite.com/2016/03/15/java-and-unicode/"/>
    <id>http://yoursite.com/2016/03/15/java-and-unicode/</id>
    <published>2016-03-15T13:55:00.000Z</published>
    <updated>2017-09-27T02:11:14.910Z</updated>
    
    <content type="html"><![CDATA[<p>优化项目时，遇到了 Unicode 一些相关的问题，为此，需要了解 Java 中是如何支持 Unicode。以下是整理的笔记。</p><img src="/2016/03/15/java-and-unicode/bg2016031503.jpg" title="Java And Unicode"><h2 id="一、Unicode-是什么？"><a href="#一、Unicode-是什么？" class="headerlink" title="一、Unicode 是什么？"></a>一、Unicode 是什么？</h2><p>Unicode 简单说来就是对世界上大部分的文字系统进行了整理，然后为他们编号。</p><p>目前最新的版本是 2015 年 6 月 17 日公布的 8.0.0，收录了 120737 个字符。</p><a id="more"></a><p>Unicode 将这些字符分成 17 组编排，每组称为平面（Plane），每平面拥有 65536（即 $$2^{16}$$）个编号，每个编号称为码点。</p><p>U+0000 至 U+FFFF 构成的平面称为<strong>基本多文种平面</strong>，英文名称 Basic Multilingual Plane，简称 <strong>BMP</strong>。最常见的字符通常都在该平面内。剩余从 U+10000 至 U+10FFFF 的 16 个平面称为辅助平面。</p><p>在表示一个 Unicode 字符时，通常会用 “U+” 然后紧接着一组十六进制的数字来表示这一个字符。</p><blockquote><p>U+4E2D = 中<br>U+1F466 = <img src="/2016/03/15/java-and-unicode/bg2016031501.png"></p></blockquote><p>使用过 emoji 的可能知道，emoji 在不同的平台（比如苹果、安卓）上的表现形式是不同的</p><img src="/2016/03/15/java-and-unicode/bg2016031502.png" title="emoji"><p>上面的都可以使用 U+1F601 表示，这也说明了 Unicode 的另一个特点，为每一个字符而非字形定义唯一的代码（码点）。</p><h2 id="二、UTF-32、UTF-16、UTF-8"><a href="#二、UTF-32、UTF-16、UTF-8" class="headerlink" title="二、UTF-32、UTF-16、UTF-8"></a>二、UTF-32、UTF-16、UTF-8</h2><p>前面提到的 U+4E2D，最简单的存储方式是使用 32 位二进制，即 4 个字节来表示, 称为 UTF-32（或 UCS-4）。</p><p>假设我们现在使用 UTF-32 对字符进行存储、传输，那么像 A（U+0041）这样的原本仅需要一个字节来表示的字符，现在需要 4 个字节，这就造成了存储空间浪费、传输效率降低。</p><p>实际上我们绝大部分使用的是基本多文众平面中的字符，也就是说，我们使用的绝大部分字符 1-2 个字节就可以表示了。</p><p>除基本平面可以用 2 个字节表示外，其它的平面都至少需要 3 个字节，因为它们的范围是 U+10000 至 U+10FFFF</p><p>如果以 16 位长的<a href="https://zh.wikipedia.org/wiki/UTF-16" target="_blank" rel="external">码元</a>（即 2 个字节为单位）表示基本平面内的字符，一个 16 位的码元就够了，但是要表示辅助平面内的字符，需要一对，称为<strong>代理对（surrogate pair）</strong>。由于长度不固定，因此这是一个变长表示。</p><table><thead><tr><th style="text-align:left">平面</th><th style="text-align:left">编码范围</th><th style="text-align:left">字节数量</th></tr></thead><tbody><tr><td style="text-align:left">基本多文种平面</td><td style="text-align:left">U+0000—U+FFFF</td><td style="text-align:left">2</td></tr><tr><td style="text-align:left">辅助平面</td><td style="text-align:left">U+10000—U+10FFFF</td><td style="text-align:left">4</td></tr></tbody></table><p>如果用 8 位长的码元（即 1 个字节为单位），需要 1 到 4 个字节，称为 <a href="https://zh.wikipedia.org/wiki/UTF-8" target="_blank" rel="external">UTF-8</a>。这也是一个变长表示。</p><table><thead><tr><th style="text-align:left">编码范围</th><th style="text-align:left">字节数量</th></tr></thead><tbody><tr><td style="text-align:left">0x0000 - 0x007F</td><td style="text-align:left">1</td></tr><tr><td style="text-align:left">0x0080 - 0x07FF</td><td style="text-align:left">2</td></tr><tr><td style="text-align:left">0x0800 - 0xFFFF</td><td style="text-align:left">3</td></tr><tr><td style="text-align:left">0x010000 - 0x10FFFF</td><td style="text-align:left">4</td></tr></tbody></table><h2 id="三、Java-对-Unicode-的支持"><a href="#三、Java-对-Unicode-的支持" class="headerlink" title="三、Java 对 Unicode 的支持"></a>三、Java 对 Unicode 的支持</h2><p>Java 使用的是 UTF-16。char 类型的长度为 2 个字节，就代表一个长度为 16 的码元。因此对于基本平面内的字符，一个 char 就可以表示一个字符。</p><figure class="highlight vbnet"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">char</span> ch = <span class="comment">'中';</span></span><br><span class="line"><span class="built_in">Integer</span>.toHexString(ch); ==&gt; <span class="number">4e2</span>d</span><br></pre></td></tr></table></figure><p>这与 “中” 的码点 U+4E2D 是一致的。</p><p>Java 提供了将码点转换为 UTF-16 形式(即用 char 来表示)的函数， java.lang.Character.toChars，该函数声明如下:</p><figure class="highlight cs"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">char</span>[] <span class="title">toChars</span>(<span class="params"><span class="keyword">int</span> codePoint</span>)</span>;</span><br></pre></td></tr></table></figure><p>使用该函数，我们看到辅助平面内的字符需要用两个char来表示：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Character.toChars(<span class="number">0x4E2D</span>).length ==&gt; <span class="number">1</span></span><br><span class="line">Character.toChars(<span class="number">0x1F466</span>).length ==&gt; <span class="number">2</span></span><br></pre></td></tr></table></figure><p>因此，原来我们统计字符个数的方法就不正确了。</p><figure class="highlight pony"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">new</span> <span class="title">String</span>(<span class="type">Character</span>.toChars(<span class="number">0x1F466</span>)).<span class="title">length</span>() ==&gt;</span> <span class="number">2</span></span><br></pre></td></tr></table></figure><p>这时，可以使用</p><figure class="highlight dart"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">String</span> s = <span class="keyword">new</span> <span class="built_in">String</span>(Character.toChars(<span class="number">0x1F466</span>));</span><br><span class="line">s.codePointCount(<span class="number">0</span>, s.length()) ==&gt; <span class="number">1</span></span><br></pre></td></tr></table></figure><p>Java 中关于使用 Character.toCodePoint 计算码点，计算方法为:</p><figure class="highlight lisp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">((<span class="name">high</span> &lt;&lt; <span class="number">10</span>) + low) + (<span class="number">0</span>x010000 - ('\uD800' &lt;&lt; <span class="number">10</span>) - '\uDC00')</span><br></pre></td></tr></table></figure><p>还有其他的一些处理码点的函数（部分）：</p><figure class="highlight stylus"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">String<span class="selector-class">.codePointAt</span> 返回指定位置的码点</span><br><span class="line">String<span class="selector-class">.offsetByCodePoints</span> 从 index 开始，接下来第n个代码点的下标</span><br><span class="line">String<span class="selector-class">.Character</span><span class="selector-class">.codePointBefore</span> 返回指定位置处前面的一个码点</span><br><span class="line">Character<span class="selector-class">.isBmpCodePoint</span> 判断一个码点是否为 BMP</span><br><span class="line">Character<span class="selector-class">.toCodePoint</span> 给定一个代理对，转换为码点</span><br><span class="line">Character<span class="selector-class">.isSupplementaryCodePoint</span> 判断代码点是否为辅助平面，即是否由两个 char 表示</span><br><span class="line">Character<span class="selector-class">.charCount</span> 判断给定码点需要几个char表示</span><br><span class="line">Character<span class="selector-class">.isSurrogatePair</span> 判断两个 char 是否为一个代理对</span><br><span class="line">Character<span class="selector-class">.isHighSurrogate</span> 判断 char 是否为代理对中的高位</span><br><span class="line">Character<span class="selector-class">.isLowSurrogate</span> 判断 char 是否为代理对中的低位</span><br></pre></td></tr></table></figure><h2 id="四、参考"><a href="#四、参考" class="headerlink" title="四、参考"></a>四、参考</h2><p><a href="https://zh.wikipedia.org/wiki/Unicode" target="_blank" rel="external">https://zh.wikipedia.org/wiki/Unicode</a><br><a href="https://zh.wikipedia.org/wiki/Unicode%E5%AD%97%E7%AC%A6%E5%B9%B3%E9%9D%A2%E6%98%A0%E5%B0%84" target="_blank" rel="external">https://zh.wikipedia.org/wiki/Unicode%E5%AD%97%E7%AC%A6%E5%B9%B3%E9%9D%A2%E6%98%A0%E5%B0%84</a><br><a href="http://apps.timwhitlock.info/emoji/tables/unicode" target="_blank" rel="external">http://apps.timwhitlock.info/emoji/tables/unicode</a><br><a href="http://www.ruanyifeng.com/blog/2007/10/ascii_unicode_and_utf-8.html" target="_blank" rel="external">http://www.ruanyifeng.com/blog/2007/10/ascii_unicode_and_utf-8.html</a><br><a href="http://www.ruanyifeng.com/blog/2014/12/unicode.html" target="_blank" rel="external">http://www.ruanyifeng.com/blog/2014/12/unicode.html</a></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;优化项目时，遇到了 Unicode 一些相关的问题，为此，需要了解 Java 中是如何支持 Unicode。以下是整理的笔记。&lt;/p&gt;
&lt;img src=&quot;/2016/03/15/java-and-unicode/bg2016031503.jpg&quot; title=&quot;Java And Unicode&quot;&gt;
&lt;h2 id=&quot;一、Unicode-是什么？&quot;&gt;&lt;a href=&quot;#一、Unicode-是什么？&quot; class=&quot;headerlink&quot; title=&quot;一、Unicode 是什么？&quot;&gt;&lt;/a&gt;一、Unicode 是什么？&lt;/h2&gt;&lt;p&gt;Unicode 简单说来就是对世界上大部分的文字系统进行了整理，然后为他们编号。&lt;/p&gt;
&lt;p&gt;目前最新的版本是 2015 年 6 月 17 日公布的 8.0.0，收录了 120737 个字符。&lt;/p&gt;
    
    </summary>
    
      <category term="java" scheme="http://yoursite.com/categories/java/"/>
    
    
      <category term="java" scheme="http://yoursite.com/tags/java/"/>
    
      <category term="unicode" scheme="http://yoursite.com/tags/unicode/"/>
    
  </entry>
  
  <entry>
    <title>Spring Oauth2 入门</title>
    <link href="http://yoursite.com/2016/02/28/spring-oauth2-introduce/"/>
    <id>http://yoursite.com/2016/02/28/spring-oauth2-introduce/</id>
    <published>2016-02-28T13:55:00.000Z</published>
    <updated>2017-09-27T02:11:29.911Z</updated>
    
    <content type="html"><![CDATA[<p>如果我们的客户端需要对用户 Github 进行一些操作，比如读取用户项目列表、上传 SSH Key 等，需要先获得 Gtihub 的授权。而 Github 使用 Oauth 2 进行授权。</p><p>本篇博客要介绍的 Spring Oauth2 是 Oauth 2.0 的实现，它简化了大部分的操作, 比如获得 access token、刷新 access token 的过程，使得通过简单配置，就可以轻松接入第三方的 Oauth 2.0 服务。</p><p>如果不了解 Oauth 2 的原理，推荐阅读<a href="http://www.ruanyifeng.com/blog/2014/05/oauth_2_0.html" target="_blank" rel="external">阮一峰的文章</a>。</p><img src="/2016/02/28/spring-oauth2-introduce/bg2016022801.png"><a id="more"></a><h2 id="一、创建-Gtihub-应用"><a href="#一、创建-Gtihub-应用" class="headerlink" title="一、创建 Gtihub 应用"></a>一、创建 Gtihub 应用</h2><p>了解完 Oauth2 原理，我们通过一个 demo，了解 spring oauth2 使用方法。</p><p>登录 Github 后，依次选择 Settings -&gt; OAuth applications -&gt; Developer applications -&gt; Register new application, 然后配置应用的信息, 依次填写 Application Name、Homepage URL、Application description、Authorization callback URL后，选择 Regiser application。</p><img src="/2016/02/28/spring-oauth2-introduce/bg2016022803.png" title="github application"><p>其中 Authorization callback URL 需要特别注意下，当我们请求 Github 授权时，有一个可选参数 <code>redirect_uri</code>，该参数指定了 Github 完成授权后重定向的网址。如果省略该参数，Github 会默认重定向到 Authorization callback URL。除此之外 Authorization callback URL 对 redirect_uri 也有一定的<a href="https://developer.github.com/v3/oauth/#redirect-urls" target="_blank" rel="external">限制作用</a>，即 redirect_uri 只能是 Authorization callback URL 的子路径。</p><p>创建完 Github 应用，我们就得到了 Github 分发的 Client Id 与 Client Secret。</p><img src="/2016/02/28/spring-oauth2-introduce/bg2016022802.png" title="IdAndSecret"><h2 id="二、为项目添加-maven-依赖"><a href="#二、为项目添加-maven-依赖" class="headerlink" title="二、为项目添加 maven 依赖"></a>二、为项目添加 maven 依赖</h2><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.security.oauth<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-security-oauth2<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.0.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure><h2 id="三、开启注解配置"><a href="#三、开启注解配置" class="headerlink" title="三、开启注解配置"></a>三、开启注解配置</h2><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">@Configuration</span></span><br><span class="line"><span class="variable">@EnableOAuth2Client</span></span><br><span class="line">public class OAuthConfig &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>该注解提供将会做两件事：</p><ol><li>注册一个 Filter（oauth2ClientContextFilter）</li><li>创建一个作用域为 request、类型为 AccessTokenRequest 的 bean。</li></ol><h2 id="三、配置客户端信息"><a href="#三、配置客户端信息" class="headerlink" title="三、配置客户端信息"></a>三、配置客户端信息</h2><p>OAuth 2.0 有四种授权方式。分别为：</p><blockquote><p>授权码模式（authorization code）<br>简化模式（implicit）<br>密码模式（resource owner password credentials）<br>客户端模式（client credentials）  </p></blockquote><p>spring 提供了 4 种 bean，依次与这四种授权方式对应，用来设定每种方式的配置信息。</p><p>这 4 个 bean 依次为：AuthorizationCodeResourceDetails、ImplicitResourceDetails、ImplicitResourceDetails、ClientCredentialsResourceDetails。</p><p>我们使用授权码模式配置 github 的 id、secret、申请认证地址、申请令牌地址、<a href="https://developer.github.com/v3/oauth/#scopes" target="_blank" rel="external">scope</a>等信息，可以这么写：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">@<span class="keyword">Bean</span></span><br><span class="line"><span class="keyword">public </span>OAuth2ProtectedResourceDetails github() &#123;</span><br><span class="line">    AuthorizationCodeResourceDetails details = new AuthorizationCodeResourceDetails()<span class="comment">;</span></span><br><span class="line">    details.setId(<span class="string">"github"</span>)<span class="comment">;</span></span><br><span class="line">    details.setClientId(<span class="string">"your client id"</span>)<span class="comment">;</span></span><br><span class="line">    details.setClientSecret(<span class="string">"your client secret"</span>)<span class="comment">;</span></span><br><span class="line">    details.setAccessTokenUri(<span class="string">"https://github.com/login/oauth/access_token"</span>)<span class="comment">;</span></span><br><span class="line">    details.setUserAuthorizationUri(<span class="string">"https://github.com/login/oauth/authorize"</span>)<span class="comment">;</span></span><br><span class="line">    details.setScope(Arrays.asList(<span class="string">"repo"</span>, <span class="string">"user:email"</span>, <span class="string">"write:public_key"</span>, <span class="string">"read:public_key"</span>))<span class="comment">;</span></span><br><span class="line">    return details<span class="comment">;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="四、配置-Oauth2Clientcontext"><a href="#四、配置-Oauth2Clientcontext" class="headerlink" title="四、配置 Oauth2Clientcontext"></a>四、配置 Oauth2Clientcontext</h2><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">@Resource</span>(name = <span class="string">"accessTokenRequest"</span>)</span><br><span class="line">private AccessTokenRequest accessTokenRequest;</span><br><span class="line"></span><br><span class="line"><span class="variable">@Bean</span></span><br><span class="line"><span class="variable">@Scope</span>(value = <span class="string">"session"</span>, proxyMode = INTERFACES)</span><br><span class="line">public OAuth2ClientContext githubOauth2ClientContext() &#123;</span><br><span class="line">    <span class="selector-tag">return</span> <span class="selector-tag">new</span> <span class="selector-tag">DefaultOAuth2ClientContext</span>(accessTokenRequest);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>accessTokenRequest 的配置信息在 <code>OAuth2ClientConfiguration</code> 中, accessTokenRequest 的作用域是 request，即每一次 HTTP 请求都会产生一个新的 accessTokenRequest。</p><p>这里的 <code>@Scope(value = &quot;session&quot;, proxyMode = INTERFACES)</code>， session 的意思是该 bean 的作用域为 session 级别，而 <code>proxyMode = INTERFACES</code> 保证了，即使该类被比作用域大于 session 的 bean 所持有，仍能保证在不同的 session，会构造不同的 OAuth2ClientContext。</p><h2 id="五、配置-OAuth2RestTemplate"><a href="#五、配置-OAuth2RestTemplate" class="headerlink" title="五、配置 OAuth2RestTemplate"></a>五、配置 OAuth2RestTemplate</h2><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable">@Bean</span></span><br><span class="line">public OAuth2RestOperations githubRestTemplate(<span class="variable">@Qualifier</span>(<span class="string">"githubOauth2ClientContext"</span>) OAuth2ClientContext clientContext) &#123;</span><br><span class="line">    <span class="selector-tag">return</span> <span class="selector-tag">new</span> <span class="selector-tag">OAuth2RestTemplate</span>(github(), clientContext);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通常，我们需要接入多个 oauth2 服务，因此会有多个 clientContext，这里我们使用 Qualifier 注解按照 bean 的名称注入。</p><p>如果我们确认只接入一个 oauth2 服务，那么就可以完全省略第四步，然后将第五步改为下面的代码：</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="keyword">public</span> <span class="function">OAuth2RestOperations <span class="title">githubRestTemplate</span><span class="params">(OAuth2ClientContext clientContext)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> OAuth2RestTemplate(github(), clientContext);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以直接注入 clientContext 的原因是 <code>OAuth2ClientConfiguration</code> 类已经默认为我们提供了。</p><h2 id="六、配置-OAuth2RestTemplate"><a href="#六、配置-OAuth2RestTemplate" class="headerlink" title="六、配置 OAuth2RestTemplate"></a>六、配置 OAuth2RestTemplate</h2><p>得到了 OAuth2RestTemplate，我们只需要使用 OAuth2RestTemplate 请求对应的服务就可以了。</p><p>比如要获取用户 github 所有的项目：</p><figure class="highlight ebnf"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attribute">JsonObject repos</span> = githubRestTemplate.getForObject(<span class="string">"https://api.github.com/user/repos"</span>, JsonObject.class);</span><br></pre></td></tr></table></figure><h2 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h2><p>demo 中包含了 Github 与 Bbitbucket 的 Oauth2.0 授权以及 Bitbucket 的 Oauth 1.0 授权配置。</p><p><a href="https://github.com/tanhe123/SpringOAuthSeample" target="_blank" rel="external">https://github.com/tanhe123/SpringOAuthSeample</a></p><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://github.com/spring-projects/spring-security-oauth" target="_blank" rel="external">https://github.com/spring-projects/spring-security-oauth</a> 中的 samples<br><a href="http://www.ruanyifeng.com/blog/2014/05/oauth_2_0.html" target="_blank" rel="external">http://www.ruanyifeng.com/blog/2014/05/oauth_2_0.html</a><br><a href="https://developer.github.com/v3/" target="_blank" rel="external">https://developer.github.com/v3/</a><br><a href="http://projects.spring.io/spring-security-oauth/docs/oauth2.html" target="_blank" rel="external">http://projects.spring.io/spring-security-oauth/docs/oauth2.html</a><br><a href="http://www.oschina.net/translate/oauth-2-developers-guide" target="_blank" rel="external">http://www.oschina.net/translate/oauth-2-developers-guide</a>  </p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;如果我们的客户端需要对用户 Github 进行一些操作，比如读取用户项目列表、上传 SSH Key 等，需要先获得 Gtihub 的授权。而 Github 使用 Oauth 2 进行授权。&lt;/p&gt;
&lt;p&gt;本篇博客要介绍的 Spring Oauth2 是 Oauth 2.0 的实现，它简化了大部分的操作, 比如获得 access token、刷新 access token 的过程，使得通过简单配置，就可以轻松接入第三方的 Oauth 2.0 服务。&lt;/p&gt;
&lt;p&gt;如果不了解 Oauth 2 的原理，推荐阅读&lt;a href=&quot;http://www.ruanyifeng.com/blog/2014/05/oauth_2_0.html&quot; target=&quot;_blank&quot; rel=&quot;external&quot;&gt;阮一峰的文章&lt;/a&gt;。&lt;/p&gt;
&lt;img src=&quot;/2016/02/28/spring-oauth2-introduce/bg2016022801.png&quot;&gt;
    
    </summary>
    
      <category term="spring" scheme="http://yoursite.com/categories/spring/"/>
    
    
      <category term="spring" scheme="http://yoursite.com/tags/spring/"/>
    
      <category term="oauth2" scheme="http://yoursite.com/tags/oauth2/"/>
    
  </entry>
  
  <entry>
    <title>动手编写 Mockito</title>
    <link href="http://yoursite.com/2016/01/10/how-implement-mockito/"/>
    <id>http://yoursite.com/2016/01/10/how-implement-mockito/</id>
    <published>2016-01-10T14:58:00.000Z</published>
    <updated>2017-09-27T02:10:20.142Z</updated>
    
    <content type="html"><![CDATA[<p>在 WebIDE 团队内部是要求写单元测试的！</p><p>这是我第一次接触Mockito。</p><p>初次使用 Mockito，能够感受到它的神奇，尤其是这样的语法：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">when(cal.add(<span class="number">0</span>, <span class="number">1</span>)).thenReturn(<span class="number">1</span>);</span><br></pre></td></tr></table></figure><p>Mockito 会把它理解成，当 cal 调用 add 方法且参数为 0 和 1 时，则返回 1。</p><p>我们知道，java 中的程序调用是以栈的形式实现的，对于 when 方法，add 方法的调用对它是不可见的。when 能接收到的，只有 add 的返回值。</p><h1 id="那么-Mockito-是如何实现的呢？"><a href="#那么-Mockito-是如何实现的呢？" class="headerlink" title="那么 Mockito 是如何实现的呢？"></a>那么 Mockito 是如何实现的呢？</h1><p>我们知道，Mock 使用了代理模式。我们操作的 cal，实际上是继承了 Calculate 的代理类的实例，我们把它称为 proxy 对象。因此对于 cal 的所有操作，都会先经过 proxy 对象。</p><p>Mocktio 就是通过这种方式，拦截到了 cal 的所有操作。只要在调用 cal 的方法时，将该方法存放起来，然后在调用 thenReturn 为其设置返回值就可以了。</p><h1 id="光说不练假把式"><a href="#光说不练假把式" class="headerlink" title="光说不练假把式"></a>光说不练假把式</h1><p>既然知道了原理，那我们也来尝试着实现一个简单的 Mockito。顺便加深下对代理模式的理解。</p><p>该 Mockito 需要支持的特性有 mock、stub、spy。</p><h2 id="第一步：-实现-mock"><a href="#第一步：-实现-mock" class="headerlink" title="第一步： 实现 mock"></a>第一步： 实现 mock</h2><p>既然要实现 mock，我们要知道，一个对象被 mock 后，有什么特征。</p><p>在单元测试中，我们要测试的模块可能依赖一些不易构造或比较复杂的对象，因此，mock 要支持通过接口、具体类构造 mock 对象。被 mock 的对象只是“假装”调用了该方法，然后“应付”的返回“空值”就可以了。</p><p>我们通过 cglib 来实现，实现代码很简单。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">T <span class="title">mock</span><span class="params">(Class&lt;T&gt; clazz)</span> </span>&#123;</span><br><span class="line">Enhancer enhancer = <span class="keyword">new</span> Enhancer();</span><br><span class="line">enhancer.setSuperclass(clazz);</span><br><span class="line">enhancer.setCallback(<span class="keyword">new</span> MockInterceptor());</span><br><span class="line"><span class="keyword">return</span> (T) enhancer.create();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">MockInterceptor</span> <span class="keyword">implements</span> <span class="title">MethodInterceptor</span> </span>&#123;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">intercept</span><span class="params">(Object obj, Method method, Object[] args, MethodProxy proxy)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line"><span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>测试用例我们可以这么写</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Calculate cal = mock(Calculate.class);</span><br><span class="line">Assert.assertEquals(<span class="number">0</span>, cal.add(<span class="number">0</span>, <span class="number">1</span>));</span><br></pre></td></tr></table></figure><h2 id="第二步，实现-stub"><a href="#第二步，实现-stub" class="headerlink" title="第二步，实现 stub"></a>第二步，实现 stub</h2><p>接下来我们来实现 stub。stub 一个方法，其实就是指定该方法在具体参数下的返回值。而且该返回值无论经过多少次调用都是不变的，除非再次 stub 该方法，用新的返回值将原来的替换掉。</p><p>首先，我们定义一个类，用来表示对一个函数的调用。</p><figure class="highlight monkey"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Invocation</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Object mock;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> <span class="function"><span class="keyword">Method</span> <span class="title">method</span>;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Object[] arguments;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> MethodProxy proxy;</span><br><span class="line"></span><br><span class="line">// 省略其它不重要代码...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>接下来，在 MockInterceptor 类中，需要做两个操作。</p><ol><li>为了设置方法的返回值，需要存放对方法的引用（lastInvocation）</li><li>调用方法时，检查是否已经设置了该方法的返回值（results）。如果设置了，则返回该值。</li></ol><p>实现代码如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">MockInterceptor</span> <span class="keyword">implements</span> <span class="title">MethodInterceptor</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">intercept</span><span class="params">(Object obj, Method method, Object[] args, MethodProxy proxy)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line"></span><br><span class="line">Invocation invocation = <span class="keyword">new</span> Invocation(proxy, method, args, proxy);</span><br><span class="line"></span><br><span class="line">lastInvocation = invocation;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (results.containsKey(invocation)) &#123;</span><br><span class="line"><span class="keyword">return</span> results.get(invocation);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">When&lt;T&gt; <span class="title">when</span><span class="params">(T o)</span> </span>&#123;</span><br><span class="line"><span class="keyword">return</span> <span class="keyword">new</span> When&lt;T&gt;();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">When</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">thenReturn</span><span class="params">(T retObj)</span> </span>&#123;</span><br><span class="line">results.put(lastInvocation, retObj);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以通过下面测试下效果：</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Calculate cal = mock(Calculate.class);</span><br><span class="line">when(cal.add(<span class="number">0</span>, <span class="number">1</span>)).thenReturn(<span class="number">1</span>);</span><br><span class="line">Assert.assertEquals(<span class="number">1</span>, cal.add(<span class="number">0</span>, <span class="number">1</span>));</span><br></pre></td></tr></table></figure><h2 id="第三步，实现-spy"><a href="#第三步，实现-spy" class="headerlink" title="第三步，实现 spy"></a>第三步，实现 spy</h2><p>如果说 mock 的处理是返回“空值”，那么 spy 的处理就是通过代理对象调用真实方法了。</p><p>我们使用 cglib 提供的方法，使得 spy 生成的代理类，默认调用真实方法。</p><figure class="highlight kotlin"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> Object callRealMethod() &#123;</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line"><span class="keyword">return</span> <span class="keyword">this</span>.proxy.invokeSuper(obj, arguments);</span><br><span class="line">&#125; <span class="keyword">catch</span> (Throwable throwable) &#123;</span><br><span class="line">throwable.printStackTrace();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>就这么简单，spy就实现了，可以使用下面的代码测试</p><figure class="highlight lsl"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Calculate cal = spy(Calculate.class);</span><br><span class="line">Assert.assertEquals(<span class="number">3</span>, cal.add(<span class="number">1</span>, <span class="number">2</span>));</span><br><span class="line">when(cal.add(<span class="number">1</span>, <span class="number">2</span>)).thenReturn(<span class="number">0</span>);</span><br><span class="line">Assert.assertEquals(<span class="number">0</span>, cal.add(<span class="number">1</span>, <span class="number">2</span>));</span><br></pre></td></tr></table></figure><p><strong>备注：限于篇幅，只贴出关键代码，可以查看源代码了解更多细节</strong></p><h1 id="源代码"><a href="#源代码" class="headerlink" title="源代码"></a>源代码</h1><p>附上示例代码: <a href="https://coding.net/u/tanhe123/p/MockitoSamples" target="_blank" rel="external">https://coding.net/u/tanhe123/p/MockitoSamples</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;在 WebIDE 团队内部是要求写单元测试的！&lt;/p&gt;
&lt;p&gt;这是我第一次接触Mockito。&lt;/p&gt;
&lt;p&gt;初次使用 Mockito，能够感受到它的神奇，尤其是这样的语法：&lt;/p&gt;
&lt;figure class=&quot;highlight lsl&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td 
      
    
    </summary>
    
      <category term="java" scheme="http://yoursite.com/categories/java/"/>
    
    
      <category term="mockito" scheme="http://yoursite.com/tags/mockito/"/>
    
  </entry>
  
  <entry>
    <title>探究 Spring Security 缓存请求</title>
    <link href="http://yoursite.com/2015/12/07/how-spring-security-cache-request/"/>
    <id>http://yoursite.com/2015/12/07/how-spring-security-cache-request/</id>
    <published>2015-12-07T14:58:00.000Z</published>
    <updated>2017-09-27T02:10:47.069Z</updated>
    
    <content type="html"><![CDATA[<h2 id="为什么要缓存"><a href="#为什么要缓存" class="headerlink" title="为什么要缓存?"></a>为什么要缓存?</h2><p>为了更好的描述问题，我们拿使用表单认证的网站举例，简化后的认证过程分为7步:</p><ol><li><p>用户访问网站，打开了一个链接(origin url)。</p></li><li><p>请求发送给服务器，服务器判断用户请求了受保护的资源。</p></li><li><p>由于用户没有登录，服务器重定向到登录页面</p></li><li><p>填写表单，点击登录</p></li><li><p>浏览器将用户名密码以表单形式发送给服务器</p></li><li><p>服务器验证用户名密码。成功，进入到下一步。否则要求用户重新认证（第三步）</p></li><li><p>服务器对用户拥有的权限（角色）判定: 有权限，重定向到 origin url; 权限不足，返回状态码 403(“forbidden”).</p></li></ol><p>从第3步，我们可以知道，用户的请求被中断了。</p><p>用户登录成功后（第7步），会被重定向到 origin url，spring security 通过使用缓存的 request，使得被中断的请求能够继续执行。</p><h2 id="使用缓存"><a href="#使用缓存" class="headerlink" title="使用缓存"></a>使用缓存</h2><p>用户登录成功后，页面重定向到 origin url。浏览器发出的请求优先被拦截器 RequestCacheAwareFilter 拦截，RequestCacheAwareFilter 通过其持有的 RequestCache 对象实现 request 的恢复。</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> doFilter(ServletRequest request, ServletResponse response,</span><br><span class="line">FilterChain chain) <span class="keyword">throws</span> IOException, ServletException &#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">// request匹配，则取出，该操作同时会将缓存的request从session中删除</span></span><br><span class="line">HttpServletRequest wrappedSavedRequest = requestCache.getMatchingRequest((HttpServletRequest) request, (HttpServletResponse) response);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 优先使用缓存的request</span></span><br><span class="line">chain.doFilter(wrappedSavedRequest == <span class="keyword">null</span> ? request : wrappedSavedRequest, response);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="何时缓存"><a href="#何时缓存" class="headerlink" title="何时缓存"></a>何时缓存</h2><p>首先，我们需要了解下 RequestCache 以及 ExceptionTranslationFilter。</p><h3 id="RequestCache"><a href="#RequestCache" class="headerlink" title="RequestCache"></a>RequestCache</h3><p>RequestCache 接口声明了缓存与恢复操作。默认实现类是 <code>HttpSessionRequestCache</code>。HttpSessionRequestCache 的实现比较简单，这里只列出接口的声明:</p><figure class="highlight cs"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title">RequestCache</span> &#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 将request缓存到session中</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">saveRequest</span>(<span class="params">HttpServletRequest request, HttpServletResponse response</span>)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 从session中取request</span></span><br><span class="line"><span class="function">SavedRequest <span class="title">getRequest</span>(<span class="params">HttpServletRequest request, HttpServletResponse response</span>)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获得与当前request匹配的缓存，并将匹配的request从session中删除</span></span><br><span class="line"><span class="function">HttpServletRequest <span class="title">getMatchingRequest</span>(<span class="params">HttpServletRequest request,</span></span></span><br><span class="line"><span class="function"><span class="params">HttpServletResponse response</span>)</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 删除缓存的request</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">removeRequest</span>(<span class="params">HttpServletRequest request, HttpServletResponse response</span>)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="ExceptionTranslationFilter"><a href="#ExceptionTranslationFilter" class="headerlink" title="ExceptionTranslationFilter"></a>ExceptionTranslationFilter</h3><p>ExceptionTranslationFilter 是 Spring Security 的核心 filter 之一，用来处理 AuthenticationException 和 AccessDeniedException 两种异常。</p><p>在我们的例子中，AuthenticationException 指的是未登录状态下访问受保护资源，AccessDeniedException 指的是登陆了但是由于权限不足(比如普通用户访问管理员界面）。</p><p>ExceptionTranslationFilter 持有两个处理类，分别是 AuthenticationEntryPoint 和 AccessDeniedHandler。</p><p>ExceptionTranslationFilter 对异常的处理是通过这两个处理类实现的，处理规则很简单：</p><blockquote><p><strong>规则1:</strong> 如果异常是 AuthenticationException，使用 AuthenticationEntryPoint 处理<br><strong>规则2:</strong> 如果异常是 AccessDeniedException 且用户是匿名用户，使用 AuthenticationEntryPoint 处理<br><strong>规则3:</strong> 如果异常是 AccessDeniedException 且用户不是匿名用户，如果否则交给 AccessDeniedHandler 处理  </p></blockquote><p>对应以下代码</p><figure class="highlight php"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> void handleSpringSecurityException(HttpServletRequest request,</span><br><span class="line">HttpServletResponse response, FilterChain chain, RuntimeException <span class="keyword">exception</span>)</span><br><span class="line">throws IOException, ServletException &#123;</span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">exception</span> <span class="keyword">instanceof</span> AuthenticationException) &#123;</span><br><span class="line">logger.debug(</span><br><span class="line"><span class="string">"Authentication exception occurred; redirecting to authentication entry point"</span>, <span class="keyword">exception</span>);</span><br><span class="line"></span><br><span class="line">sendStartAuthentication(request, response, chain, (AuthenticationException) <span class="keyword">exception</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">exception</span> <span class="keyword">instanceof</span> AccessDeniedException) &#123;</span><br><span class="line"><span class="keyword">if</span> (authenticationTrustResolver.isAnonymous(SecurityContextHolder.getContext().getAuthentication())) &#123;</span><br><span class="line">logger.debug(<span class="string">"Access is denied (user is anonymous); redirecting to authentication entry point"</span>,<span class="keyword">exception</span>);</span><br><span class="line"></span><br><span class="line">sendStartAuthentication(request, response, chain, <span class="keyword">new</span> InsufficientAuthenticationException(<span class="string">"Full authentication is required to access this resource"</span>));</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line">logger.debug(</span><br><span class="line"><span class="string">"Access is denied (user is not anonymous); delegating to AccessDeniedHandler"</span>, <span class="keyword">exception</span>);</span><br><span class="line"></span><br><span class="line">accessDeniedHandler.handle(request, response, (AccessDeniedException) <span class="keyword">exception</span>);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>AccessDeniedHandler 默认实现是 AccessDeniedHandlerImpl。该类对异常的处理是返回403错误码。</p><figure class="highlight aspectj"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> handle(HttpServletRequest request, HttpServletResponse response,</span><br><span class="line">AccessDeniedException accessDeniedException) <span class="keyword">throws</span> IOException,</span><br><span class="line">ServletException &#123;</span><br><span class="line"><span class="keyword">if</span> (!response.isCommitted()) &#123;</span><br><span class="line"><span class="keyword">if</span> (errorPage != <span class="keyword">null</span>) &#123;  <span class="comment">// 定义了errorPage</span></span><br><span class="line"><span class="comment">// errorPage中可以操作该异常</span></span><br><span class="line">request.setAttribute(WebAttributes.ACCESS_DENIED_403,</span><br><span class="line">accessDeniedException);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置403状态码</span></span><br><span class="line">response.setStatus(HttpServletResponse.SC_FORBIDDEN);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 转发到errorPage</span></span><br><span class="line">RequestDispatcher dispatcher = request.getRequestDispatcher(errorPage);</span><br><span class="line">dispatcher.forward(request, response);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123; <span class="comment">// 没有定义errorPage，则返回403状态码(Forbidden)，以及错误信息</span></span><br><span class="line">response.sendError(HttpServletResponse.SC_FORBIDDEN,</span><br><span class="line">accessDeniedException.getMessage());</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>AuthenticationEntryPoint 默认实现是 LoginUrlAuthenticationEntryPoint, 该类的处理是转发或重定向到登录页面</p><figure class="highlight lasso"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="literal">void</span> commence(HttpServletRequest request, HttpServletResponse response,</span><br><span class="line">AuthenticationException authException) throws IOException, ServletException &#123;</span><br><span class="line"></span><br><span class="line"><span class="built_in">String</span> redirectUrl = <span class="built_in">null</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (useForward) &#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (forceHttps &amp;&amp; <span class="string">"http"</span>.<span class="keyword">equals</span>(request.getScheme())) &#123;</span><br><span class="line"><span class="comment">// First redirect the current request to HTTPS.</span></span><br><span class="line"><span class="comment">// When that request is received, the forward to the login page will be</span></span><br><span class="line"><span class="comment">// used.</span></span><br><span class="line">redirectUrl = buildHttpsRedirectUrlForRequest(request);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (redirectUrl == <span class="built_in">null</span>) &#123;</span><br><span class="line"><span class="built_in">String</span> loginForm = determineUrlToUseForThisRequest(request, response,</span><br><span class="line">authException);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">logger.debug(<span class="string">"Server side forward to: "</span> + loginForm);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">RequestDispatcher dispatcher = request.getRequestDispatcher(loginForm);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 转发</span></span><br><span class="line">dispatcher.forward(request, response);</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span>;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> &#123;</span><br><span class="line"><span class="comment">// redirect to login page. Use https if forceHttps true</span></span><br><span class="line">redirectUrl = buildRedirectUrlToLoginPage(request, response, authException);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 重定向</span></span><br><span class="line">redirectStrategy.sendRedirect(request, response, redirectUrl);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>了解完这些，回到我们的例子。</p><p>第3步时，用户未登录的情况下访问受保护资源，ExceptionTranslationFilter 会捕获到 AuthenticationException 异常(规则1)。页面需要跳转，ExceptionTranslationFilter 在跳转前使用 requestCache 缓存 request。</p><figure class="highlight less"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">protected</span> <span class="selector-tag">void</span> <span class="selector-tag">sendStartAuthentication</span>(HttpServletRequest request,</span><br><span class="line">HttpServletResponse response, FilterChain chain,</span><br><span class="line">AuthenticationException reason) <span class="selector-tag">throws</span> <span class="selector-tag">ServletException</span>, <span class="selector-tag">IOException</span> &#123;</span><br><span class="line"><span class="comment">// SEC-112: Clear the SecurityContextHolder's Authentication, as the</span></span><br><span class="line"><span class="comment">// existing Authentication is no longer considered valid</span></span><br><span class="line"><span class="selector-tag">SecurityContextHolder</span><span class="selector-class">.getContext</span>()<span class="selector-class">.setAuthentication</span>(null);</span><br><span class="line"><span class="comment">// 缓存 request</span></span><br><span class="line"><span class="selector-tag">requestCache</span><span class="selector-class">.saveRequest</span>(request, response);</span><br><span class="line"><span class="selector-tag">logger</span><span class="selector-class">.debug</span>(<span class="string">"Calling Authentication entry point."</span>);</span><br><span class="line"><span class="selector-tag">authenticationEntryPoint</span><span class="selector-class">.commence</span>(request, response, reason);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="一些坑"><a href="#一些坑" class="headerlink" title="一些坑"></a>一些坑</h2><p>在开发过程中，如果不理解 Spring Security 如何缓存 request，可能会踩一些坑。</p><p>举个简单例子，如果网站认证是信息存放在 header 中。第一次请求受保护资源时，请求头中不包含认证信息 ，验证失败，该请求会被缓存，之后即使用户填写了信息，也会因为request被恢复导致信息丢失从而认证失败(问题描述可以参见<a href="http://stackoverflow.com/questions/33568237/how-to-disable-or-override-requestcacheawarefilter-in-spring-boot" target="_blank" rel="external">这里</a>。</p><p>最简单的方案当然是不缓存request。</p><p>spring security 提供了 NullRequestCache， 该类实现了 RequestCache 接口，但是没有任何操作。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">NullRequestCache</span> <span class="keyword">implements</span> <span class="title">RequestCache</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> SavedRequest <span class="title">getRequest</span><span class="params">(HttpServletRequest request,</span></span></span><br><span class="line"><span class="function"><span class="params">HttpServletResponse response)</span> </span>&#123;</span><br><span class="line"><span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">removeRequest</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">saveRequest</span><span class="params">(HttpServletRequest request, HttpServletResponse response)</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> HttpServletRequest <span class="title">getMatchingRequest</span><span class="params">(HttpServletRequest request,</span></span></span><br><span class="line"><span class="function"><span class="params">HttpServletResponse response)</span> </span>&#123;</span><br><span class="line"><span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>配置 requestCache，使用如下代码即可:</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">http</span><span class="selector-class">.requestCache</span>()<span class="selector-class">.requestCache</span>(<span class="selector-tag">new</span> <span class="selector-tag">NullRequestCache</span>());</span><br></pre></td></tr></table></figure><h2 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h2><p>默认情况下，三种 request 不会被缓存。</p><ol><li>请求地址以 <code>/favicon.ico</code> 结尾</li><li>header中的 <code>content-type</code> 值为 <code>application/json</code></li><li>header中的 <code>X-Requested-With</code> 值为 <code>XMLHttpRequest</code></li></ol><p>可以参见：<code>RequestCacheConfigurer</code> 类中的私有方法 <code>createDefaultSavedRequestMatcher</code>。</p><p>附上实例代码: <a href="https://coding.net/u/tanhe123/p/SpringSecurityRequestCache" target="_blank" rel="external">https://coding.net/u/tanhe123/p/SpringSecurityRequestCache</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;为什么要缓存&quot;&gt;&lt;a href=&quot;#为什么要缓存&quot; class=&quot;headerlink&quot; title=&quot;为什么要缓存?&quot;&gt;&lt;/a&gt;为什么要缓存?&lt;/h2&gt;&lt;p&gt;为了更好的描述问题，我们拿使用表单认证的网站举例，简化后的认证过程分为7步:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
      
    
    </summary>
    
      <category term="spring" scheme="http://yoursite.com/categories/spring/"/>
    
    
      <category term="spring security" scheme="http://yoursite.com/tags/spring-security/"/>
    
  </entry>
  
</feed>
